diff --git a/web_app/ServiceController.py b/web_app/ServiceController.py
index be31a62..c908997 100644
--- a/web_app/ServiceController.py
+++ b/web_app/ServiceController.py
@@ -1,6 +1,7 @@
 import os
 from pystemd.systemd1 import Unit
 from pystemd.systemd1 import Manager
+from pystemd.dbusexc import DBusError
 
 class ServiceController(object):
     """
@@ -16,9 +17,21 @@ class ServiceController(object):
         self.unit = Unit(bytes(unit, 'utf-8'), _autoload=True)
         
     def isActive(self):
-        if self.unit.Unit.ActiveState == b'active':
+        last_exception = None
+
+        for _ in range(3):
+            try:
+                activeState = self.unit.Unit.ActiveState
+                break
+            except DBusError as e:
+                last_exception = e
+                time.sleep(0.001)
+        else:
+            raise last_exception
+
+        if activeState == b'active':
             return True
-        elif self.unit.Unit.ActiveState == b'activating':
+        elif activeState == b'activating':
             #TODO manage this transitionnal state differently
             return True
         else:
