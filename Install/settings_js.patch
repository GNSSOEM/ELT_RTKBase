diff --git a/web_app/static/settings.js b/web_app/static/settings.js
index 5b64baa..4a42cc4 100644
--- a/web_app/static/settings.js
+++ b/web_app/static/settings.js
@@ -24,6 +24,8 @@
 
 // ############################### MAIN ###############################
 
+let expectedServerRestart = false;
+
 $(document).ready(function () {
 
 	if(window.location.hash != '')
@@ -41,13 +43,21 @@ $(document).ready(function () {
 
     // say hello on connect
     socket.on("connect", function () {
+        console.log("connect");
         socket.emit("browser connected", {data: "I'm connected"});
+        if (expectedServerRestart) {
+           expectedServerRestart = false;
+           $("#updateModal .modal-title").text("Restarting web server");
+           $("#updateModal .modal-body").html('The web server restarted.');
+           $("#start-update-button").prop("disabled", false);
+           $("#start-update-button").off("click");
+           $("#start-update-button").on("click", function() {
+               location.reload();
+           });
+        };
     });
     console.log("main.js Asking for service status");
     socket.emit("get services status");
-    socket.on('disconnect', function(){
-        console.log('disconnected');
-    });
 
     //Enable "Save" button when form value is changed
     $("form").on('input', 'input, select', function() {
@@ -79,7 +89,7 @@ $(document).ready(function () {
     // View/hide password buttons for: Ntrip A, Ntrip B and Local caster
     document.querySelectorAll(".input-group-append").forEach(function(e) {
         var name = e.querySelector("button").id.replace("_button", "");
-        if (!["svr_pwd_A", "svr_pwd_B", "local_ntripc_pwd", "rtcm_client_pwd"].includes(name))
+        if (!["svr_pwd_A", "svr_pwd_B", "svr_pwd_C", "svr_pwd_D", "svr_pwd_E", "local_ntripc_pwd", "rtcm_client_pwd"].includes(name))
             return;
 
         var button = $("#" + name + "_button");
@@ -111,7 +121,7 @@ $(document).ready(function () {
     socket.on("services status", function(msg) {
         // gestion des services
         var servicesStatus = JSON.parse(msg);
-        //console.log("service status: " + servicesStatus);
+        //console.log("service status: " + msg);
         
         // ################ MAiN service Switch  ######################
         //console.log("REFRESHING  service switch");
@@ -131,7 +141,12 @@ $(document).ready(function () {
         if (servicesStatus[0].btn_off_color) {
             mainSwitch.bootstrapToggle('setOffStyle', servicesStatus[0].btn_off_color);
         }
-        
+        var main_Error = $('#main-error');
+        if (servicesStatus[0].error) {
+            main_Error.text(servicesStatus[0].error);
+        } else {
+            main_Error.text('');
+        }
         // event for switching on/off service on user mouse click
         //TODO When the switch changes its position, this event seems attached before
         //the switch finish its transition, then fire another event.
@@ -159,6 +174,12 @@ $(document).ready(function () {
         if (servicesStatus[1].btn_off_color) {
             ntrip_A_Switch.bootstrapToggle('setOffStyle', servicesStatus[1].btn_off_color);
         }
+        var ntrip_A_Error = $('#ntrip_A-error');
+        if (servicesStatus[1].error) {
+            ntrip_A_Error.text(servicesStatus[1].error);
+        } else {
+            ntrip_A_Error.text('');
+        }
         
         // event for switching on/off service on user mouse click
         //TODO When the switch changes its position, this event seems attached before
@@ -187,6 +208,12 @@ $(document).ready(function () {
         if (servicesStatus[2].btn_off_color) {
             ntrip_B_Switch.bootstrapToggle('setOffStyle', servicesStatus[2].btn_off_color);
         }
+        var ntrip_B_Error = $('#ntrip_B-error');
+        if (servicesStatus[2].error) {
+            ntrip_B_Error.text(servicesStatus[2].error);
+        } else {
+            ntrip_B_Error.text('');
+        }
         
         // event for switching on/off service on user mouse click
         //TODO When the switch changes its position, this event seems attached before
@@ -198,11 +225,113 @@ $(document).ready(function () {
             socket.emit("services switch", {"name" : "ntrip_B", "active" : switchStatus});           
         })
 
+        // ####################  NTRIP C service Switch #########################
+        var ntrip_C_Switch = $('#ntrip_C-switch');
+        // set the switch to on/off depending of the service status
+        if (servicesStatus[3].active === true) {
+            //document.querySelector("#main-switch").bootstrapToggle('on');
+            ntrip_C_Switch.bootstrapToggle('on', true);
+        } else {
+            //document.querySelector("#main-switch").bootstrapToggle('off');
+            ntrip_C_Switch.bootstrapToggle('off', true);
+        }
+        //console.log(servicesStatus[3]);
+        if (servicesStatus[3].btn_color) {
+            ntrip_C_Switch.bootstrapToggle('setOnStyle', servicesStatus[3].btn_color);
+        }
+        if (servicesStatus[3].btn_off_color) {
+            ntrip_C_Switch.bootstrapToggle('setOffStyle', servicesStatus[3].btn_off_color);
+        }
+        var ntrip_C_Error = $('#ntrip_C-error');
+        if (servicesStatus[3].error) {
+            ntrip_C_Error.text(servicesStatus[3].error);
+        } else {
+            ntrip_C_Error.text('');
+        }
+        
+        // event for switching on/off service on user mouse click
+        //TODO When the switch changes its position, this event seems attached before
+        //the switch finish its transition, then fire another event.
+        $( "#ntrip_C-switch" ).one("change", function(e) {
+            var switchStatus = $(this).prop('checked');
+            //console.log(" e : " + e);
+            //console.log("Ntrip SwitchStatus : " + switchStatus);
+            socket.emit("services switch", {"name" : "ntrip_C", "active" : switchStatus});           
+        })
+
+        // ####################  NTRIP D service Switch #########################
+        var ntrip_D_Switch = $('#ntrip_D-switch');
+        // set the switch to on/off depending of the service status
+        if (servicesStatus[4].active === true) {
+            //document.querySelector("#main-switch").bootstrapToggle('on');
+            ntrip_D_Switch.bootstrapToggle('on', true);
+        } else {
+            //document.querySelector("#main-switch").bootstrapToggle('off');
+            ntrip_D_Switch.bootstrapToggle('off', true);
+        }
+        //console.log(servicesStatus[4]);
+        if (servicesStatus[4].btn_color) {
+            ntrip_D_Switch.bootstrapToggle('setOnStyle', servicesStatus[4].btn_color);
+        }
+        if (servicesStatus[4].btn_off_color) {
+            ntrip_D_Switch.bootstrapToggle('setOffStyle', servicesStatus[4].btn_off_color);
+        }
+        var ntrip_D_Error = $('#ntrip_D-error');
+        if (servicesStatus[4].error) {
+            ntrip_D_Error.text(servicesStatus[4].error);
+        } else {
+            ntrip_D_Error.text('');
+        }
+        
+        // event for switching on/off service on user mouse click
+        //TODO When the switch changes its position, this event seems attached before
+        //the switch finish its transition, then fire another event.
+        $( "#ntrip_D-switch" ).one("change", function(e) {
+            var switchStatus = $(this).prop('checked');
+            //console.log(" e : " + e);
+            //console.log("Ntrip SwitchStatus : " + switchStatus);
+            socket.emit("services switch", {"name" : "ntrip_D", "active" : switchStatus});           
+        })
+
+        // ####################  NTRIP E service Switch #########################
+        var ntrip_E_Switch = $('#ntrip_E-switch');
+        // set the switch to on/off depending of the service status
+        if (servicesStatus[5].active === true) {
+            //document.querySelector("#main-switch").bootstrapToggle('on');
+            ntrip_E_Switch.bootstrapToggle('on', true);
+        } else {
+            //document.querySelector("#main-switch").bootstrapToggle('off');
+            ntrip_E_Switch.bootstrapToggle('off', true);
+        }
+        //console.log(servicesStatus[5]);
+        if (servicesStatus[5].btn_color) {
+            ntrip_E_Switch.bootstrapToggle('setOnStyle', servicesStatus[5].btn_color);
+        }
+        if (servicesStatus[5].btn_off_color) {
+            ntrip_E_Switch.bootstrapToggle('setOffStyle', servicesStatus[5].btn_off_color);
+        }
+        var ntrip_E_Error = $('#ntrip_E-error');
+        if (servicesStatus[5].error) {
+            ntrip_E_Error.text(servicesStatus[5].error);
+        } else {
+            ntrip_E_Error.text('');
+        }
+        
+        // event for switching on/off service on user mouse click
+        //TODO When the switch changes its position, this event seems attached before
+        //the switch finish its transition, then fire another event.
+        $( "#ntrip_E-switch" ).one("change", function(e) {
+            var switchStatus = $(this).prop('checked');
+            //console.log(" e : " + e);
+            //console.log("Ntrip SwitchStatus : " + switchStatus);
+            socket.emit("services switch", {"name" : "ntrip_E", "active" : switchStatus});           
+        })
+
         // ################  Local NTRIP Caster service Switch #####################
 
         var ntripcSwitch = $('#ntripc-switch');
         // set the switch to on/off depending of the service status
-        if (servicesStatus[3].active === true) {
+        if (servicesStatus[6].active === true) {
             //document.querySelector("#main-switch").bootstrapToggle('on');
             ntripcSwitch.bootstrapToggle('on', true);
         } else {
@@ -210,12 +339,12 @@ $(document).ready(function () {
             ntripcSwitch.bootstrapToggle('off', true);
         }
         
-        //console.log(servicesStatus[2]);
-        if (servicesStatus[3].btn_color) {
-            ntripcSwitch.bootstrapToggle('setOnStyle', servicesStatus[3].btn_color);
+        //console.log(servicesStatus[6]);
+        if (servicesStatus[6].btn_color) {
+            ntripcSwitch.bootstrapToggle('setOnStyle', servicesStatus[6].btn_color);
         }
-        if (servicesStatus[3].btn_off_color) {
-            ntripcSwitch.bootstrapToggle('setOffStyle', servicesStatus[3].btn_off_color);
+        if (servicesStatus[6].btn_off_color) {
+            ntripcSwitch.bootstrapToggle('setOffStyle', servicesStatus[6].btn_off_color);
         }
         
         // event for switching on/off service on user mouse click
@@ -232,19 +361,19 @@ $(document).ready(function () {
 
         var rtcmSvrSwitch = $('#rtcm_svr-switch');
         // set the switch to on/off depending of the service status
-        if (servicesStatus[4].active === true) {
+        if (servicesStatus[7].active === true) {
             //document.querySelector("#main-switch").bootstrapToggle('on');
             rtcmSvrSwitch.bootstrapToggle('on', true);
         } else {
             //document.querySelector("#main-switch").bootstrapToggle('off');
             rtcmSvrSwitch.bootstrapToggle('off', true);
         }
-        //console.log(servicesStatus[3]);
-        if (servicesStatus[4].btn_color) {
-            rtcmSvrSwitch.bootstrapToggle('setOnStyle', servicesStatus[4].btn_color);
+        //console.log(servicesStatus[7]);
+        if (servicesStatus[7].btn_color) {
+            rtcmSvrSwitch.bootstrapToggle('setOnStyle', servicesStatus[7].btn_color);
         }
-        if (servicesStatus[4].btn_off_color) {
-            rtcmSvrSwitch.bootstrapToggle('setOffStyle', servicesStatus[4].btn_off_color);
+        if (servicesStatus[7].btn_off_color) {
+            rtcmSvrSwitch.bootstrapToggle('setOffStyle', servicesStatus[7].btn_off_color);
         }
         
         // event for switching on/off service on user mouse click
@@ -261,19 +390,19 @@ $(document).ready(function () {
         
         var rtcmSerialSwitch = $('#rtcm_serial-switch');
         // set the switch to on/off depending of the service status
-        if (servicesStatus[5].active === true) {
+        if (servicesStatus[8].active === true) {
             //document.querySelector("#main-switch").bootstrapToggle('on');
             rtcmSerialSwitch.bootstrapToggle('on', true);
         } else {
             //document.querySelector("#main-switch").bootstrapToggle('off');
             rtcmSerialSwitch.bootstrapToggle('off', true);
         }
-        //console.log(servicesStatus[4]);
-        if (servicesStatus[5].btn_color) {
-            rtcmSerialSwitch.bootstrapToggle('setOnStyle', servicesStatus[5].btn_color);
+        //console.log(servicesStatus[8]);
+        if (servicesStatus[8].btn_color) {
+            rtcmSerialSwitch.bootstrapToggle('setOnStyle', servicesStatus[8].btn_color);
         }
-        if (servicesStatus[5].btn_off_color) {
-            rtcmSerialSwitch.bootstrapToggle('setOffStyle', servicesStatus[5].btn_off_color);
+        if (servicesStatus[8].btn_off_color) {
+            rtcmSerialSwitch.bootstrapToggle('setOffStyle', servicesStatus[8].btn_off_color);
         }
         
         // event for switching on/off service on user mouse click
@@ -290,19 +419,19 @@ $(document).ready(function () {
 
         var fileSwitch = $('#file-switch');
         // set the switch to on/off depending of the service status
-        if (servicesStatus[6].active === true) {
+        if (servicesStatus[9].active === true) {
             //document.querySelector("#main-switch").bootstrapToggle('on');
             fileSwitch.bootstrapToggle('on', true);
         } else {
             //document.querySelector("#main-switch").bootstrapToggle('off');
             fileSwitch.bootstrapToggle('off', true);
         }
-        //console.log(servicesStatus[5]);
-        if (servicesStatus[6].btn_color) {
-            fileSwitch.bootstrapToggle('setOnStyle', servicesStatus[6].btn_color);
+        //console.log(servicesStatus[9]);
+        if (servicesStatus[9].btn_color) {
+            fileSwitch.bootstrapToggle('setOnStyle', servicesStatus[9].btn_color);
         }
-        if (servicesStatus[6].btn_off_color) {
-            fileSwitch.bootstrapToggle('setOffStyle', servicesStatus[6].btn_off_color);
+        if (servicesStatus[9].btn_off_color) {
+            fileSwitch.bootstrapToggle('setOffStyle', servicesStatus[9].btn_off_color);
         }
         
         // event for switching on/off service on user mouse click
@@ -343,8 +472,8 @@ $(document).ready(function () {
     socket.on("gnss_detection_result", function(msg) {
         // open modal box with detection result and asking for configuration if detection is a success
         response = JSON.parse(msg);
-        console.log(response);
-        detectApplyBtnElt.setAttribute('data-dismiss', 'modal');
+        //console.log(response);
+        //detectApplyBtnElt.setAttribute('data-dismiss', 'modal');
         if (response['result'] === 'success') {
             detectBodyElt.innerHTML = '<b>' + response['gnss_type'] + ' ' + response['model'] + '</b>' + ' detected on ' + '<b>' + response['port'] + '</b>' + '<br>' + '<br>' + 'Do you want to apply?';
             detectApplyBtnElt.onclick = function (){
@@ -361,7 +490,7 @@ $(document).ready(function () {
         response = JSON.parse(msg);
         if (response['then_configure']) {
             // asking for gnss receiver configuration
-        document.querySelector('#configure_receiver_button').click();
+            document.querySelector('#configure_receiver_button').click();
         } else {
             // refreshing the page to display the new informations
             location.href = document.URL.replace(/#$/, '');
@@ -381,18 +510,18 @@ $(document).ready(function () {
     })
     socket.on("gnss_configuration_result", function(msg) {
         response = JSON.parse(msg);
-        console.log(response);
+        //console.log(response);
         detectApplyBtnElt.removeAttribute('disabled');
-        detectApplyBtnElt.setAttribute('data-dismiss', 'modal');
+        //detectApplyBtnElt.setAttribute('data-dismiss', 'modal');
+        detectApplyBtnElt.removeAttribute('data-dismiss');
         detectApplyBtnElt.innerText = "Close";
+        detectApplyBtnElt.onclick = function() {
+            // window.location.reload();
+            // looks like this way works better :
+            location.href = document.URL.replace(/#$/, '');
+        }
         if (response['result'] === 'success') {
             detectBodyElt.innerHTML = "GNSS receiver successfully configured!";
-            detectApplyBtnElt.removeAttribute('data-dismiss');
-            detectApplyBtnElt.onclick = function() {
-                // window.location.reload();
-                // looks like this way works better :
-                location.href = document.URL.replace(/#$/, '');
-            }
         } else {
             detectBodyElt.innerHTML = "GNSS receiver configuration failed"
         }
@@ -412,28 +541,47 @@ $(document).ready(function () {
     // ####################### HANDLE UPDATE #######################
 
     $('#check_update_button').on("click", function (){
-        socket.emit("check update");
+        $("#start-update-button").html('Update');
+        $("#start-update-button").prop("disabled", true);
+        $("#update-cancel-button").html('Cancel');
+        $("#update-cancel-button").on("click", null);
+        $("#update-cancel-button").prop("disabled", false);
+        $("#updateModal .modal-body").text('');
+        const urlParams = new URLSearchParams(window.location.search);
+        update = urlParams.get('update');
+        if (update === 'manual') update = null;
+        console.log("check update ELT. branch=" + update);
+        socket.emit("check update ELT", {"branch" : update});
     });
 
     socket.on("new release", function(msg) {
         // open modal box asking for starting update
         response = JSON.parse(msg);
-        console.log(JSON.stringify(response));
+        console.log("new release:" + JSON.stringify(response));
         if (response.error) {
             $("#updateModal .modal-title").text("Update error!");
-            $("#updateModal .modal-body").append(response['error']);
+            $("#updateModal .modal-body").text(response['error']);
             $("#updateModal").modal();
         }else if (response.new_release) {
             $("#updateModal .modal-title").text("Update available!");
-            $("#updateModal .modal-body").append('<p class="text-center">Do you want to install RTKBase <b>' + response['new_release'] +'</b>? <br>It will take a few minutes.</p>');                    
-            $("#updateModal .modal-body").append('<p id="newFeatures"></p>');
+            $("#updateModal .modal-body").append('<p class="text-center">Do you want to install ELT RTKBase <b>' + response['new_release'] +'</b>? <br>It will take a few minutes.</p>');                    
             var markdwnHtml = snarkdown(response['comment']);
-            //lower <Hx> title tags values
-                markdwnHtml= markdwnHtml.replace(/<h([1-4])(.*?)>(.*?)<\/h\1>/gi, (match, p1, p2, p3) => {
-                    const newHtag = Math.min(parseInt(p1) + 3, 6);
-                    return `<h${newHtag}${p2}>${p3}</h${newHtag}>`;
-                });
-            $("#newFeatures").append('<p>' + markdwnHtml + '</p>');     
+            if (!markdwnHtml.includes("<li>")) {
+               markdwnHtml = markdwnHtml.replace(/\r\n/g, '<br>');
+               var newFeaturesArray = response['comment'].split('\r\n');
+               $("#updateModal .modal-body").append('<p><ul id="newFeatures"><strong>New features</strong>:</ul></p><br>');
+               $.each( newFeaturesArray, function( index, value ){
+                   $("#newFeatures").append("<li>" + value.replace(/^\+ /g, "") + "</li>");
+               });
+            } else {
+               //lower <Hx> title tags values
+               markdwnHtml= markdwnHtml.replace(/<h([1-4])(.*?)>(.*?)<\/h\1>/gi, (match, p1, p2, p3) => {
+                   const newHtag = Math.min(parseInt(p1) + 3, 6);
+                   return `<h${newHtag}${p2}>${p3}</h${newHtag}>`;
+               });
+               $("#updateModal .modal-body").append('<p id="newFeatures"></p>');
+               $("#newFeatures").append('<p>' + markdwnHtml + '</p>');
+            }
             $("#start-update-button").removeAttr("disabled");
             $("#updateModal").modal();
         } else {
@@ -445,55 +593,68 @@ $(document).ready(function () {
         const queryString = window.location.search;
         const urlParams = new URLSearchParams(queryString);
         if (urlParams.get('update') === 'manual' ) {
-            $("#updateModal .modal-body").append('<h3>Manual Update: </h3><form method="POST" action="" enctype="multipart/form-data"> <p><input type="file" name="file"></p><p><input id="submit-button" type="submit" value="Submit"></p></form>');
+            $("#updateModal .modal-body").append('<h3>Manual Update: </h3><form method="POST" action="" enctype="multipart/form-data"> <p><input id="submit-name" type="file" name="file"></p><p><input id="submit-button" type="submit" value="Submit"></p></form>');
             const upd_formElt = document.querySelector('#updateModal div.modal-body form');
+            submit_name = document.getElementById("submit-name");
+            $("#submit-button").prop("disabled", true);
+            submit_name.addEventListener('change', function(ev) {
+                console.log('submit_name changed to:<'+submit_name.value+'>');
+                $("#submit-button").prop("disabled", (submit_name.value == ""));
+            });
             upd_formElt.addEventListener('submit', handleFileSubmit);
             function handleFileSubmit(event) {
-                console.log('inside function handlefilesubmit');
                 event.preventDefault;
                 fetch(upd_formElt.action, {
                     method: "post",
                     // body: new URLSearchParams(new FormData(form)) // for application/x-www-form-urlencoded
                     body: new FormData(upd_formElt) // for multipart/form-data
                 });
-                $("#updateModal .modal-body").html('<span class="spinner-border spinner-border-sm"></span> Updating...');
+                $("#updateModal .modal-title").text("Manual Update from file!");
+                $("#updateModal .modal-body").text("Please wait...Downloading update from file...");
+                $("#start-update-button").prop("disabled", true);
+                $("#start-update-button").html('<span class="spinner-border spinner-border-sm"></span> Updating...');
+                $("#update-cancel-button").prop("disabled", true);
             };
         }
     });
     
     $("#start-update-button").on("click", function () {
-        //$("#updateModal .modal-title").text(("Installing update"));
-        socket.emit("update rtkbase");
+        $("#updateModal .modal-title").text("Installing update");
+        const urlParams = new URLSearchParams(window.location.search);
+        update = urlParams.get('update');
+        if (update === 'manual') update = null;
+        console.log("update rtkbase ELT. branch=" + update);
+        socket.emit("update rtkbase ELT", {"branch" : "main"});
         $("#updateModal .modal-body").text("Please wait...Downloading update...");
         $(this).prop("disabled", true);
         $(this).html('<span class="spinner-border spinner-border-sm"></span> Updating...');
-        $("#cancel-button").prop("disabled", true);
+        $("#update-cancel-button").prop("disabled", true);
         //now, we waiting for a download result message from the server
     })
 
     socket.on("downloading_update", function(msg) {
         response = JSON.parse(msg);
-        console.log("Downloading result: " + response);
+        console.log("Downloading result: " + JSON.stringify(response));
         if (response['result'] === 'true') {
             $("#updateModal .modal-body").text("Please wait...Preparing update...");
         } else {
-            $("#updateModal .modal-body").text("Download failure");
+            $("#updateModal .modal-body").text(response['error']);
             $("#start-update-button").html('Update...');
-            $("#cancel-button").prop("disabled", false);
+            $("#update-cancel-button").prop("disabled", false);
         }
     })
 
     socket.on("updating_rtkbase_stopped", function(msg) {
         response = JSON.parse(msg);
-        console.log("mgs: " + response.error)
-        $("#updateModal .modal-title").text("Error !");
+        console.log("updating_rtkbase_stopped: " + JSON.stringify(response))
+        $("#updateModal .modal-title").text(response.result);
         $("#updateModal .modal-body").text("");
         for (line of response.error) {
-            $("#updateModal .modal-body").append("<p>" + line + "</p>");
+            $("#updateModal .modal-body").append(line + "<br>");
         }
-        $("#start-update-button").html('Update');
+        $("#start-update-button").html('Update...');
         $("#start-update-button").prop("disabled", true);
-        $("#cancel-button").prop("disabled", false);
+        $("#update-cancel-button").prop("disabled", false);
     })
 
     socket.on("updating_rtkbase", function() {
@@ -501,16 +662,69 @@ $(document).ready(function () {
         //update_countdown(1200, 0);
     })
     
-    socket.on("update_successful", function() {
-            console.log("update successful");
+    function set_restart_window(reason) {
+        $("#updateModal .modal-title").text("Restarting web server");
+        $("#updateModal .modal-body").html(
+            '<span class="spinner-border spinner-border-sm"></span> ' +
+             'The web server ' + reason + '...'
+        );
+
+        $("#start-update-button").html('Refresh');
+        $("#start-update-button").prop("disabled", true);
+        $("#update-cancel-button").html('Cancel');
+        $("#update-cancel-button").prop("disabled", true);
+        expectedServerRestart = true;
+        $("#updateModal").modal();
+    }
+
+    socket.on("server_restarting", function(data) {
+        console.log("Server will be restarting:",  data && data.reason);
+        set_restart_window("is about to restart. Please wait")
+    });
+
+    socket.on('disconnect', function(){
+        console.log('disconnected');
+        set_restart_window("is restarting. Waiting for reconnection")
+    });
+
+    socket.on("update_successful", function(msg) {
+        expectedServerRestart = false;
+        socket.emit("update successful ack");
+
+        $("#updateModal .modal-title").text("Update finished");
+        response = JSON.parse(msg);
+        console.log("update finished: " + JSON.stringify(response));
+        console.log("result=" + response['result']);
+        $("#start-update-button").html('Refresh');
+        $("#start-update-button").off("click");
+        $("#start-update-button").on("click", function() {
+            location.reload();
+        });
+        if (response['result'] === 'true')
             $("#updateModal .modal-body").text("Update Successful!");
-            $("#start-update-button").html('Refresh');
-            $("#start-update-button").prop("disabled", false);
+        else if (response['result'] === 'false')
+            $("#updateModal .modal-body").text("Update Error! See update.log in Logs page");
+        else if (response['result'] === 'reboot') {
+            $("#updateModal .modal-body").text("Update Successful, please REBOOT!");
             $("#start-update-button").off("click");
             $("#start-update-button").on("click", function() {
+               $('#updateModal').modal("hide");
+               $("#rebootModal").modal();
+               $("#confirm-reboot-button").click();
+            });
+            $("#start-update-button").html('Reboot');
+            $("#update-cancel-button").html('Refresh');
+            $("#update-cancel-button").on("click", function() {
                 location.reload();
             });
-            $("#updateModal").modal();
+        } else {
+            $("#updateModal .modal-body").html('Update Error! See ' + 
+                                              '<a href="/logs/download/' + response['result'] + '" target="_blank">' +
+                                               response['result'] + '</a> in Logs page');
+        }
+        $("#start-update-button").prop("disabled", false);
+        $("#update-cancel-button").prop("disabled", false);
+        $("#updateModal").modal();
     });
 
     function update_countdown(remaining, count) {
@@ -527,14 +741,6 @@ $(document).ready(function () {
         }
         setTimeout(function(){ update_countdown(remaining - 1, count + 1); }, 1000);
     };
-    // Cleaning update modal box when closing it
-
-    $("#updateModal").on('hidden.bs.modal', function(){
-        $("#updateModal .modal-title").text("Update");
-        $("#updateModal .modal-body").text('');
-        $("#start-update-button").prop("disabled", true);
-      });
-
     // ####################### HANDLE CHANGING PASSWORD #######################
 
     document.getElementById('change_password').addEventListener("input", function(e) {
@@ -597,33 +803,9 @@ $(document).ready(function () {
             volumeSpaceElt.style.color = "#212529";
         }
 
-        var networkElt = document.getElementById("network_infos");
-        networkElt.innerHTML = createNetworkInterfacesList(sysInfos["network_infos"]);
+        // Per-interface network details moved to the Network manager dialog header
+        // (wifiRenderIfaces); the settings page keeps only the "Manage" button.
     })
-
-    function createNetworkInterfacesList(interfaces) {
-        let html = '<dl>'
-        interfaces.forEach(interface => {
-        html += `<dt style="font-weight: initial;">${interface.device}`;
-        if (interface.conn_name) {
-            html += ` (${interface.conn_name})`;
-        }
-        html += '</dt>';
-        html += '<dd><ul>';
-        if (interface.ipv4) {
-            html += `<li>IPv4: ${interface.ipv4.join(' - ')}</li>`;
-        }
-        if (interface.ipv6) {
-            html += `<li>IPv6: ${interface.ipv6.join(' - ')}</li>`;
-        }
-        if (interface.hwaddr) {
-            html += `<li>MAC: ${interface.hwaddr}</li>`;
-        }
-        html += '</ul></dd>';
-        });
-        html += '</dl>';
-        return html;
-    }
     //source: https://stackoverflow.com/a/34270811
     /**
      * Translates seconds into human readable format of seconds, minutes, hours, days, and years
@@ -726,6 +908,23 @@ $(document).ready(function () {
           };
       };
 
+    // ########################### HANDLE TAILSCALE ###########################
+
+    document.getElementById('tailscale-button').onclick = function (e){
+        socket.emit("get tailscale href");
+        document.body.style.cursor = 'wait';
+        e.preventDefault();
+        $('#tailscale-error').text('');
+    }
+    socket.on("tailscale_href", function(msg) {
+        window.open(msg, '_blank');
+        document.body.style.cursor = 'default';
+    })
+    socket.on("tailscale_error", function(msg) {
+        $('#tailscale-error').text(msg);
+        document.body.style.cursor = 'default';
+    })
+
     // ####################### HANDLE REBOOT & SHUTDOWN #######################
 
     $("#reboot-button").on("click", function() {
@@ -737,16 +936,19 @@ $(document).ready(function () {
         $(this).prop("disabled", true);
         $("#reboot-cancel-button").prop("disabled", true);
         socket.emit("reboot device");
-        reboot_countdown(90, 0);
+        reboot_countdown(90, 0, 0);
     })
 
-    function reboot_countdown(remaining, count) {
+    function reboot_countdown(remaining, count, count2) {
         if(remaining === 0)
             location.reload();
         if (count > 15 && socket.connected)
-            location.reload();
-        document.getElementById('countdown').innerHTML = remaining;
-        setTimeout(function(){ reboot_countdown(remaining - 1, count + 1); }, 1000);
+           count2=count2+1;
+        if (count2 > 20)
+           location.reload();
+        if (remaining >= 0)
+           document.getElementById('countdown').innerHTML = remaining;
+        setTimeout(function(){ reboot_countdown(remaining - 1, count + 1, count2); }, 1000);
     };
     $("#shutdown-button").on("click", function() {
         $("#shutdownModal").modal();
@@ -758,5 +960,1797 @@ $(document).ready(function () {
         socket.emit("shutdown device");
     })
 
+    $("#upgrade-button").on("click", function() {
+        $("#upgradeModal .modal-body").html('Update & upgrade linux. Are you sure?');
+        $("#confirm-upgrade-button").prop("hidden", false);
+        $("#confirm-upgrade-button").prop("disabled", false);
+        $("#confirm-upgrade-button").html('Upgrade');
+        $("#upgrade-cancel-button").prop("hidden", false);
+        $("#upgrade-cancel-button").prop("disabled", false);
+        $("#upgrade-cancel-button").html('Cancel');
+        $("#upgradeModal").modal();
+    })
+    $("#confirm-upgrade-button").on("click", function() {
+        $("#upgradeModal .modal-body").html('<div class="align-items-center">Upgrading now...  <div class="spinner-border ml-auto" role="status" aria-hidden="true"></div></div>');
+        $(this).html('<span class="spinner-border spinner-border-sm"></span> Upgrading...');
+        $(this).prop("disabled", true);
+        $("#upgrade-cancel-button").prop("hidden", true);
+        socket.emit("update and upgrade");
+    })
+    socket.on("upgrade_finished", function(msg) {
+        $("#upgradeModal .modal-body").html(msg);
+        $("#confirm-upgrade-button").prop("hidden", true);
+        $("#upgrade-cancel-button").prop("hidden", false);
+        $("#upgrade-cancel-button").html('Continue');
+    })
+    function set_ntrip_mode(letter) {
+        const selectElem = document.getElementById("svr_mode_" + letter);
+        if (selectElem == null)
+           return
+        const index = selectElem.selectedIndex;
+        // https://developer.mozilla.org/en-US/docs/Web/API/HTMLSelectElement/selectedIndex
+        //console.log(selectElem.name + "=" + index);
+        svr_addr = document.getElementById("svr_addr_" + letter);
+        svr_login = $("#svr_login_" + letter + "_group");
+        svr_input = $("#svr_login_" + letter);
+        svr_pwd = $("#svr_pwd_" + letter + "_group");
+        pwd_input = $("#svr_pwd_" + letter);
+        mnt_name = $("#mnt_name_" + letter + "_group");
+        mnt_input = $("#mnt_name_" + letter);
+        rtcm_msg = $("#rtcm_msg_" + letter + "_group");
+        receiver_options = $("#ntrip_" + letter + "_receiver_options_group");
+        switch(index) {
+           case 2: // NTRIP v2 TCP
+           case 3: // NTRIP v2 RSTP
+           case 4: // NTRIP v2 UDP
+                   svr_login.prop("hidden", false);
+                   svr_pwd.prop("hidden", false);
+                   pwd_input.prop("required", true);
+                   mnt_name.prop("hidden", false);
+                   if ((svr_addr.value === "servers.onocoy.com") && (index === 2)) {
+                      //console.log("Onocoy v2 mode");
+                      mnt_input.prop("required", false);
+                      svr_input.prop("required", true);
+                   } else {
+                      mnt_input.prop("required", true);
+                      svr_input.prop("required", false);
+                   }
+                   rtcm_msg.prop("hidden", true);
+                   receiver_options.prop("hidden", true);
+                   break;
+           case 1: // TCP
+                   svr_input.prop("required", false);
+                   svr_login.prop("hidden", true);
+                   pwd_input.prop("required", false);
+                   svr_pwd.prop("hidden", true);
+                   mnt_input.prop("required", false);
+                   mnt_name.prop("hidden", true);
+                   rtcm_msg.prop("hidden", false);
+                   receiver_options.prop("hidden", false);
+                   break;
+           default: // NTRIP v1
+                   //console.log(svr_addr.value);
+                   if (svr_addr.value === "servers.onocoy.com") {
+                      //console.log("Onocoy v1 mode");
+                      svr_login.prop("hidden", false);
+                      svr_input.prop("required", true);
+                      mnt_input.prop("required", false);
+                      mnt_name.prop("hidden", true);
+                   } else {
+                      svr_input.prop("required", false);
+                      svr_login.prop("hidden", true);
+                      mnt_name.prop("hidden", false);
+                      mnt_input.prop("required", true);
+                   }
+                   svr_pwd.prop("hidden", false);
+                   pwd_input.prop("required", true);
+                   rtcm_msg.prop("hidden", false);
+                   receiver_options.prop("hidden", false);
+        }
+    }
+
+    document.querySelectorAll("select.form-control").forEach(function(e) {
+        if (/svr_mode_/.test(e.id)) {
+           var letter = e.id.replace("svr_mode_", "");
+           //console.log(e.id + "(" + letter + ")");
+           e.addEventListener('click', function(ev) {
+               set_ntrip_mode(letter);
+           });
+           e.click()
+        }
+    });
+
+    document.querySelectorAll("input.form-control").forEach(function(e) {
+        if (/svr_addr_/.test(e.id)) {
+           var letter = e.id.replace("svr_addr_", "");
+           //console.log(e.id + "(" + letter + ")");
+           e.addEventListener('change', function(ev) {
+               set_ntrip_mode(letter);
+           });
+        }
+    });
+
+    function set_rtcm_mode() {
+       receiver_format = document.getElementById("receiver_format");
+       isRtcm = receiver_format.value === "rtcm3";
+       //console.log("set_rtcm_mode=" + isRtcm  + "(" + receiver_format.value + ")");
+       $("#rtcm_svr_msg_group").prop("hidden", isRtcm);
+       document.querySelectorAll("select.form-control").forEach(function(e) {
+          if (/svr_mode_/.test(e.id)) {
+             //console.log(e.id);
+             if (!isRtcm && (e.selectedIndex > 1))
+                e.selectedIndex= 0;
+             e.options[2].hidden = !isRtcm;
+             e.options[3].hidden = !isRtcm;
+             e.options[4].hidden = !isRtcm;
+          }
+       });
+    };
+
+    document.querySelectorAll("input.form-control").forEach(function(e) {
+       if (["rtcm_msg_A", "rtcm_msg_B", "rtcm_msg_C", "rtcm_msg_D", "rtcm_msg_E", "local_ntripc_msg", "rtcm_svr_msg", "rtcm_serial_msg"].includes(e.id)) {
+          var cb = Combobox('#'+e.id, {
+               options: [
+                  "1005(10),1033(10),1004,1012",
+                  "1006(10),1033(10),1077,1087,1097,1127,1230(10)",
+                  "1005(10),1033(10),1077,1087,1097,1107,1117,1127,1137,1230(30)",
+                  "1005(30),1033(30),1077,1087,1097,1107,1117,1127,1137,1230(30)",
+                  "1005(10),1077,1087,1097,1107,1117,1127,1137,1230(30)",
+                  "1005(10),1006(30),1007(30),1013(30),1033(10),1019,1020,1041,1042,1044,1045,1046,1048,1077,1087,1097,1107,1117,1127,1137,1230(30)",
+                  "1005,1006,1007,1013,1033,1019,1020,1041,1042,1044,1045,1046,1048,1077,1087,1097,1107,1117,1127,1137,1230"
+                ]
+          });
+       };
+    });
+
+    //main_service_save = $("#main_service_save");
+    $("#main_service_save").on('click', set_rtcm_mode);
+
+    set_rtcm_mode();
+
+    $('#ext_web_site').on("click", function (e){
+        e.preventDefault();
+        socket.emit("mosaic available");
+    })
+
+    socket.on("mosaic_available_result", function(msg) {
+        if (msg) {
+           ext_web_site = document.getElementById("ext_web_site");
+           href = ext_web_site.parentElement.href;
+           window.open(href, '_blank');
+        } else  {
+           $("#warningModal .modal-body").text('Mosaic Web server is unavailable because the address 192.168.3.1 belongs to a different network.');
+           $('#warningModal').modal();
+        }
+    })
+
+
+    $('#modem_web_site').on("click", function (e){
+        e.preventDefault();
+        socket.emit("modem available");
+    })
+
+    socket.on("modem_available_result", function(msg) {
+        if (msg) {
+           modem_web_site = document.getElementById("modem_web_site");
+           href = modem_web_site.parentElement.href;
+           window.open(href, '_blank');
+        } else  {
+           $("#warningModal .modal-body").text('Modem Web server is unavailable because the address 192.168.8.1 belongs to a different network.');
+           $('#warningModal').modal();
+        }
+    })
+
+
+    // ############### WiFi manager ###############
+
+    var wifi = {
+        status: null,        // "wifi status" payload: capability/interfaces/country/channels/bands/occupied
+        list: null,          // "wifi list" payload: radio/country/role/ap_active/connected/saved/available
+        countriesLoaded: false,
+        updating: false,     // guard: programmatic form updates must not emit
+        scanning: false,     // a scan is in flight — show the "Scanning…" placeholder
+        busy: false,         // a blocking operation is running — status persists, controls locked
+        busyDone: '',        // message to flash (auto-hiding) when the current busy op finishes
+        busyTerminal: '',    // event that finishes the current busy op: 'result' | 'list' | 'ap'
+        apHasPassword: false, // the AP profile has a stored key (the key itself never reaches the browser)
+        apStoredEnabled: false, // the point's own toggle as the backend last reported it
+        apStoredAuto: false,    // the Auto (watchdog) toggle as stored in wifi_manager.conf
+        apPendingAuto: false,   // Auto switched on in the form, arms on Save
+        apConfigured: false,    // the AP profile exists (the point was saved at least once)
+        apFormOpen: false,      // the operator opened the sleeping point's form to edit it
+        apStoredSecurity: '',   // security as last rendered — start-field change detection
+        apOnAir: false,         // the beacon is in the air right now
+        apRaisedByAuto: false,  // ...and it is the watchdog's own, not the operator's
+        cm: {mode: 'connect', ssid: '', profile: null, security: null}
+    };
+    var wifiToastTimer = null;
+    var wifiAutoScan = null;
+
+    // While the popup is open, silently re-scan periodically so networks that go off the air
+    // stop showing a stale signal (their bars age out). Skipped when it would disrupt: a
+    // blocking op running, radio off, AP mode, or an open dropdown/dialog.
+    function wifiStartAutoScan() {
+        clearInterval(wifiAutoScan);
+        wifiAutoScan = setInterval(function () {
+            if (wifi.busy || !(wifi.list && wifi.list.radio)) return;
+            // Skip the tick while the user has something open that a re-render would snap shut:
+            // an open row menu (custom-toggled .show, lives in the net list), the country
+            // selectpicker, or the connect dialog. NOTE: bootstrap-select leaves .dropdown-menu.show
+            // on its (closed) menu with aria-expanded=false, so match the selectpicker by
+            // aria-expanded, not by the stale .show — otherwise the tick was skipped forever.
+            if ($('#wifi-net-list .dropdown-menu.show').length
+                || $('#wifiModal [aria-expanded="true"]').length
+                || $('#wifiConnectModal').is(':visible')) return;
+            // silent: no "Scanning…" placeholder. Also ticks in AP mode — the point lives
+            // on its own interface, so the client interface still scans under a running
+            // access point, and the beacon does not blink.
+            socket.emit('wifi rescan');
+        }, 15000);
+    }
+
+    function wifiEsc(s) {
+        return $('<span>').text(s == null ? '' : String(s)).html();
+    }
+
+    // Floating toast, bottom-center (prototype): info auto-hides, errors linger a bit longer.
+    function wifiToast(msg, isError) {
+        if (wifi.busy) return;   // a blocking operation owns the toast until it finishes
+        var t = $('#wifi-toast');
+        t.text(msg).toggleClass('wifi-toast-error', !!isError).show();
+        clearTimeout(wifiToastTimer);
+        wifiToastTimer = setTimeout(function () { t.hide(); }, isError ? 12000 : 2500);
+    }
+
+    // Long operation in progress: persistent spinner toast + block all controls so the user
+    // can't interrupt it mid-way (which would raise a confusing error). Cleared ONLY by the
+    // operation's own terminal event (`terminal`: 'result' | 'list' | 'ap') or 'wifi error' —
+    // an unrelated "wifi list" from the background rescan must NOT unlock a running connect.
+    function wifiSetBusy(on, msg, doneMsg, terminal) {
+        if (on) {
+            wifi.busy = true;
+            wifi.busyDone = doneMsg || '';
+            wifi.busyTerminal = terminal || 'list';
+            $('#wifiModal').addClass('wifi-busy');
+            clearTimeout(wifiToastTimer);
+            $('#wifi-toast').html('<span class="spinner-border spinner-border-sm"></span>' + wifiEsc(msg || 'Working…'))
+                .removeClass('wifi-toast-error').show();
+        } else {
+            if (!wifi.busy) return;
+            wifi.busy = false;
+            wifi.busyTerminal = '';
+            $('#wifiModal').removeClass('wifi-busy');
+            $('#wifi-toast').hide();
+            var done = wifi.busyDone; wifi.busyDone = '';
+            if (done) wifiToast(done);               // auto-hiding confirmation
+        }
+    }
+    function wifiBusyDoneIf(terminal) {              // clear busy only when its terminal event arrives
+        if (wifi.busy && wifi.busyTerminal === terminal) wifiSetBusy(false);
+    }
+
+    // Password rules per security kind. Returns an error string or null.
+    function wifiValidatePwd(sec, pwd) {
+        if (!sec || sec === 'open') return null;
+        pwd = pwd || '';
+        if (sec === 'wep') {
+            // Any length is accepted: 5/13 characters and 10/26 hex digits are literal keys,
+            // anything else is handed to the adapter as a passphrase it derives the key from
+            // (an access point with a 12-digit key was impossible to enter before).
+            if (!pwd.length) return 'Enter the WEP key';
+            if (pwd.length > 64) return 'WEP key or passphrase must be 64 characters or fewer';
+            return null;
+        }
+        if (pwd.length < 8 || pwd.length > 63) return 'Password must be 8–63 characters';
+        return null;
+    }
+    // Password-format hint under the key field: the expected format differs per security
+    // kind (WEP keys vs WPA passphrases) and a mismatch is not obvious from the error alone.
+    function wifiPwdSetHint(sec) {
+        $('#wifi-cm-pwd-hint').text(
+            !sec || sec === 'open' ? '' :
+            sec === 'wep' ? 'Enter the key exactly as the access point shows it. A WEP key '
+                          + 'itself is 5 or 13 characters, or the same key written as 10 or 26 '
+                          + 'hex digits (0-9, a-f). Any other length — 12 digits, for example — '
+                          + 'is a passphrase the access point derives the key from, and is '
+                          + 'accepted as such.'
+                          : 'Passphrase: 8–63 characters (64 hex digits for a raw key)');
+    }
+    function wifiShowPwdErr(msg) {
+        $('#wifi-cm-pwd-err').text(msg || '').toggle(!!msg);
+        $('#wifi-cm-pwd').css('border-color', msg ? '#dc3545' : '');
+        if (msg) $('#wifi-cm-pwd').focus();
+    }
+
+    function wifiBars(sig) {
+        sig = sig == null ? 0 : sig;
+        var n = sig >= 66 ? 4 : sig >= 40 ? 3 : sig >= 20 ? 2 : 1;
+        var col = sig >= 66 ? '#2e7d32' : sig >= 40 ? '#f9a825' : '#9e9e9e';
+        var s = '<svg width="22" height="18" viewBox="0 0 22 18" style="flex:none">';
+        for (var i = 0; i < 4; i++) {
+            var h = 5 + i * 4, x = i * 5 + 1, y = 16 - h;
+            s += '<rect x="' + x + '" y="' + y + '" width="3.4" height="' + h + '" rx="1" fill="' + (i < n ? col : '#dee2e6') + '"/>';
+        }
+        return s + '</svg>';
+    }
+
+    // ---- IPv4 helpers (live validation) ----
+    function wifiIpToInt(ip) {
+        var p = (ip || '').trim().split('.');
+        if (p.length !== 4) return null;
+        var n = 0;
+        for (var i = 0; i < p.length; i++) {
+            if (!/^\d{1,3}$/.test(p[i])) return null;
+            var v = +p[i];
+            if (v > 255) return null;
+            n = n * 256 + v;
+        }
+        return n >>> 0;
+    }
+    function wifiNetAddr(ipInt, prefix) {
+        var mask = prefix === 0 ? 0 : (0xFFFFFFFF << (32 - prefix)) >>> 0;
+        return (ipInt & mask) >>> 0;
+    }
+    function wifiPrefixToMask(p) {
+        var m = [];
+        for (var i = 0; i < 4; i++) {
+            var b = Math.max(0, Math.min(8, p - i * 8));
+            m.push(256 - Math.pow(2, 8 - b));
+        }
+        return m.join('.');
+    }
+    function wifiMaskOptions(sel) {
+        var o = '';
+        for (var p = 8; p <= 30; p++) {
+            o += '<option value="' + p + '"' + (p === sel ? ' selected' : '') + '>' + wifiPrefixToMask(p) + ' (/' + p + ')</option>';
+        }
+        return o;
+    }
+    // first overlapping occupied ethernet subnet (from NM via backend) or null
+    function wifiOccupiedConflict(ipInt, prefix) {
+        var occ = (wifi.status && wifi.status.occupied) || [];
+        for (var i = 0; i < occ.length; i++) {
+            var parts = occ[i].net.split('/');
+            var oIp = wifiIpToInt(parts[0]), oPrefix = +parts[1];
+            if (oIp === null) continue;
+            var mp = Math.min(prefix, oPrefix);
+            if (wifiNetAddr(ipInt, mp) === wifiNetAddr(oIp, mp)) return occ[i];
+        }
+        return null;
+    }
+    // shared IPv4 validation; ids = {ip, mask, gw?, dns?, err}
+    function wifiValidateStatic(ids) {
+        var errEl = $('#' + ids.err), ipEl = $('#' + ids.ip);
+        var prefix = +$('#' + ids.mask).val();
+        function fail(el, m) {
+            errEl.text('⚠ ' + m).removeClass('text-warning').addClass('text-danger').show();
+            if (el) el.css('border-color', '#dc3545');
+            return false;
+        }
+        function good() {
+            if (warnMsg) errEl.text('⚠ ' + warnMsg).removeClass('text-danger').addClass('text-warning').show();
+            else errEl.removeClass('text-warning').addClass('text-danger').hide();
+            ipEl.css('border-color', '');
+            if (ids.gw) $('#' + ids.gw).css('border-color', '');
+            return true;
+        }
+        var ip = wifiIpToInt(ipEl.val());
+        if (ip === null) return fail(ipEl, 'Invalid IP address.');
+        var net = wifiNetAddr(ip, prefix);
+        var host = (prefix === 0 ? 0xFFFFFFFF : (~((0xFFFFFFFF << (32 - prefix)) >>> 0)) >>> 0);
+        var bcast = (net | host) >>> 0;
+        if (ip === net || ip === bcast) return fail(ipEl, 'This is a network or broadcast address — use a host address.');
+        var conf = wifiOccupiedConflict(ip, prefix);
+        // the device name says WHERE the subnet is; the profile name ("Wired connection 2")
+        // does not, and the operator has to guess which cable that is
+        var confWhere = conf ? (conf.dev || conf.conn) : '';
+        if (conf && !ids.softOccupied) return fail(ipEl, 'Subnet overlaps ' + conf.net + ' (' + confWhere + '). Choose a different subnet.');
+        // client STA: overlapping an occupied subnet can be intentional (Ethernet and
+        // WiFi in one L2 network with a shared DHCP) — warn without blocking
+        var warnMsg = conf ? 'Subnet overlaps ' + conf.net + ' (' + confWhere + ') — make sure the networks are really one (shared DHCP), otherwise routing breaks.' : null;
+        // exact clash with an address already assigned to this device (header interfaces
+        // list; skip wlan0 — re-using its own current address is fine — and the form's own
+        // interface (ids.selfDevice): saving the access point while it runs must not trip
+        // over the very address the point itself holds on ap0)
+        var ifs = (wifi.status && wifi.status.interfaces) || [];
+        for (var k = 0; k < ifs.length; k++) {
+            if (ifs[k].device === 'wlan0' || ifs[k].device === ids.selfDevice || !ifs[k].ipv4) continue;
+            for (var m = 0; m < ifs[k].ipv4.length; m++) {
+                if (wifiIpToInt(ifs[k].ipv4[m]) === ip)
+                    return fail(ipEl, 'Address ' + ifs[k].ipv4[m] + ' is already used by this device (' + ifs[k].device + ').');
+            }
+        }
+        if (ids.gw) {
+            var gwVal = $('#' + ids.gw).val().trim();
+            if (gwVal) {
+                var gw = wifiIpToInt(gwVal);
+                if (gw === null) return fail($('#' + ids.gw), 'Invalid gateway address.');
+                if (wifiNetAddr(gw, prefix) !== net) return fail($('#' + ids.gw), 'Gateway is not in the same subnet as the IP address.');
+                if (gw === ip) return fail(ipEl, 'Address must differ from the gateway.');
+            }
+        }
+        if (ids.dns) {
+            var dnsVal = $('#' + ids.dns).val().trim();
+            if (dnsVal) {
+                var list = dnsVal.split(',');
+                for (var i = 0; i < list.length; i++) {
+                    var t = list[i].trim();
+                    if (t && wifiIpToInt(t) === null) return fail($('#' + ids.dns), 'Invalid DNS address: ' + t);
+                }
+            }
+        }
+        return good();
+    }
+
+    // ---- radio / channels ----
+    // No 802.11 Mode selector: NetworkManager (1.42) has no profile property for the AP
+    // standard or channel width — a Mode choice changed nothing on the radio and only
+    // misled (agreed with the client to drop it). The channel list is simply every
+    // IR-allowed standard channel of the band.
+    function wifiApChannels(band) {
+        var all = ((wifi.status && wifi.status.channels) || {})[band] || [];
+        var outdoor = !!(wifi.status && wifi.status.outdoor);
+        var chans = [];
+        for (var i = 0; i < all.length; i++) {
+            var e = all[i];
+            if (e.disabled || e.no_ir || e.dfs) continue;
+            // an outdoor station may not use channels the regulator marks indoor-only
+            if (outdoor && e.no_outdoor) continue;
+            // declared unusable for this station's adapter in wifi_manager.conf: the firmware
+            // refuses the channel even though every regulatory table allows it
+            if (e.unavailable) continue;
+            // `iw list` also enumerates non-standard 5 GHz center frequencies (e.g. ch 34,
+            // the Japanese legacy J52 grid) — the AP cannot start on those; keep only the
+            // standard 20 MHz grid: 36–144 and 149–177 in steps of 4
+            if (band === '5' && !((e.ch >= 36 && e.ch <= 144 && (e.ch - 36) % 4 === 0) ||
+                                  (e.ch >= 149 && e.ch <= 177 && (e.ch - 149) % 4 === 0))) continue;
+            chans.push(e.ch);
+        }
+        return chans;
+    }
+    function wifiChanFreq(ch) {
+        return ch <= 14 ? (ch === 14 ? 2484 : 2407 + ch * 5) : 5000 + ch * 5;
+    }
+    function wifiRepopRadio() {
+        var band = $('#wifi-ap-band').val();
+        var cSel = $('#wifi-ap-channel');
+        if (band === 'all') {
+            cSel.prop('disabled', true).html('<option value="0">Auto</option>');
+            $('#wifi-ap-chan-warn').text('').hide();
+            // outdoor-nowhere не зависит от диапазона — пересчитываем и здесь, иначе
+            // надпись залипает после правки конфига (п.93) и не появляется при Auto (п.90)
+            if (wifi.status && wifi.status.outdoor_nowhere) {
+                $('#wifi-ap-outdoor-note').text('In region '
+                    + ((wifi.status && wifi.status.country) || '—') + ' the regulator marks '
+                    + 'every band indoor-only, and this station is configured as outdoor — an '
+                    + 'access point cannot run here at all. Client connections are not affected.')
+                    .show();
+            } else {
+                $('#wifi-ap-outdoor-note').text('').hide();
+            }
+            wifiApRadioLock();
+            wifiApOneVoice();
+            return;
+        }
+        cSel.prop('disabled', !!wifi.apFormLock);
+        var chans = wifiApChannels(band), prevCh = cSel.val();
+        cSel.html('<option value="0">Auto</option>' + chans.map(function (ch) {
+            return '<option value="' + ch + '"' + (String(ch) === prevCh ? ' selected' : '') + '>ch ' + ch + ' · ' + wifiChanFreq(ch) + ' MHz</option>';
+        }).join(''));
+        // A channel stored in the profile that the adapter cannot beacon on (inherited from
+        // provisioning or an import — our picker never offers one) has no option here. Keep
+        // it visible and flagged instead of silently showing "Auto", which hid the real
+        // setting and let Save rewrite it behind the user's back. Re-applied on every
+        // repopulate because a fresh 'wifi status' rebuilds the list.
+        var stored = wifi.apStoredChannel;
+        if (stored && band === wifi.apStoredBand && chans.indexOf(stored) < 0) {
+            cSel.append('<option value="' + stored + '" selected>ch ' + stored + ' · ' +
+                wifiChanFreq(stored) + ' MHz — not available</option>');
+            $('#wifi-ap-chan-warn').text('⚠ Channel ' + stored + ' is stored in the profile but not ' +
+                'available on this adapter in the current region — the access point cannot start on it. ' +
+                'Pick another channel or Auto.').show();
+        } else {
+            $('#wifi-ap-chan-warn').text('').hide();
+        }
+        // Outdoor station and the regulator leaves nothing usable in this band: say so instead
+        // of showing a picker with only "Auto" in it.
+        var cc = (wifi.status && wifi.status.country) || '';
+        if (wifi.status && wifi.status.outdoor_nowhere) {
+            // every band is indoor-only here — saying "use the other band" would be a lie
+            $('#wifi-ap-outdoor-note').text('In region ' + (cc || '—') + ' the regulator marks '
+                + 'every band indoor-only, and this station is configured as outdoor — an '
+                + 'access point cannot run here at all. Client connections are not affected.')
+                .show();
+        } else if (!chans.length && wifi.status && wifi.status.outdoor) {
+            $('#wifi-ap-outdoor-note').text('Outdoor Wi-Fi is not allowed on ' + band +
+                ' GHz in region ' + (cc || '—') + ' — this station is configured as outdoor. ' +
+                'Use the other band.').show();
+        } else {
+            $('#wifi-ap-outdoor-note').text('').hide();
+        }
+        wifiApRadioLock();
+        wifiApOneVoice();
+    }
+
+    function wifiApRadioLock() {
+        // решение по п.52: при живом клиентском подключении выбор диапазона/канала
+        // иллюзорен — точка следует за клиентом; селекторы блокируются с текущими
+        // значениями (Save их не теряет), выбор возвращается без клиента
+        var conn = (wifi.list && wifi.list.connected) || [];
+        var lock = !!conn.length && !wifi.apFormLock;
+        $('#wifi-ap-band').prop('disabled', lock || !!wifi.apFormLock);
+        if ($('#wifi-ap-band').val() !== 'all') {
+            $('#wifi-ap-channel').prop('disabled', lock || !!wifi.apFormLock);
+        }
+        var chTxt = conn.length && conn[0].chan
+            ? (conn[0].band ? conn[0].band + ' GHz ch ' + conn[0].chan : 'ch ' + conn[0].chan)
+            : '';
+        $('#wifi-ap-radio-lock-note').text(lock
+            ? 'A network is connected' + (chTxt ? ' on ' + chTxt : '')
+              + ' — the access point follows its channel. Band and channel unlock when '
+              + 'nothing is connected.'
+            : '');
+    }
+
+    function wifiApOneVoice() {
+        // п.87: канальные строки говорили хором, местами противоречиво — в форме звучит
+        // ОДНА, по приоритету; фактический канал всегда виден в лампе шапки
+        var order = ['#wifi-ap-illegal', '#wifi-ap-chan-warn', '#wifi-ap-outdoor-note',
+                     '#wifi-ap-radio-lock-note', '#wifi-ap-channel-note', '#wifi-ap-air'];
+        var winner = null;
+        order.forEach(function (id) {
+            var el = $(id);
+            if (!winner && el.text().trim()) {
+                winner = id;
+                // clear our priority-hide first so the stylesheet lays the line out again
+                if (el[0]) el[0].style.removeProperty('display');
+                el.show();
+            } else {
+                el.hide();
+                // a utility class carrying display:...!important (bootstrap's d-block did)
+                // beats the plain inline none that hide() writes and the "hidden" line
+                // stays on screen — hide with priority so the verdict wins regardless
+                if (el[0]) el[0].style.setProperty('display', 'none', 'important');
+            }
+        });
+    }
+
+    // ---- rendering ----
+    function wifiRenderIfaces() {
+        var ifaces = (wifi.status && wifi.status.interfaces) || null;
+        if (!ifaces || !ifaces.length) { $('#wifi-ifaces').html(''); return; }
+        var muted = function (s) { return '<span class="text-muted">' + s + '</span>'; };
+        var rows = ifaces.map(function (itf) {
+            var v4list = (itf.ipv4 || []).map(wifiEsc);
+            var v6list = (itf.ipv6 || []).map(wifiEsc);
+            var connected = v4list.length || v6list.length;
+            var v4 = connected ? (v4list.join('<br>') || muted('—')) : muted('not connected');
+            // dash leads each continuation line so a wrapped pair still reads as a range (a – b)
+            var v6 = v6list.length ? v6list.join('<br>– ') : muted('—');
+            // connection name under the device; when unset it falls back to the device name
+            var dev = '<span class="wifi-if-dev">' + wifiEsc(itf.device) + '</span>' +
+                '<br><span class="wifi-if-conn">' + wifiEsc(itf.conn_name || itf.device) + '</span>';
+            return '<tr>' +
+                '<td class="wifi-if-name">' + dev + '</td>' +
+                '<td>' + v4 + '</td>' +
+                '<td>' + v6 + '</td>' +
+                '<td>' + wifiEsc(itf.hwaddr || '—') + '</td>' +
+                '</tr>';
+        }).join('');
+        $('#wifi-ifaces').html(
+            '<table class="wifi-if-table">' +
+            '<colgroup><col style="width:22%"><col style="width:24%"><col style="width:36%"><col style="width:18%"></colgroup>' +
+            '<thead><tr><th>Interface</th><th>IPv4</th><th>IPv6</th><th>MAC</th></tr></thead>' +
+            '<tbody>' + rows + '</tbody></table>');
+    }
+
+    function wifiLimitedMsg(entry) {
+        return entry.usable === 'disabled'
+            ? "This network's channel is not permitted in " + wifi.list.country + ' — switch WiFi region to connect'
+            : 'This channel is not allowed without a WiFi region — select a region to connect';
+    }
+
+    // Menu entry toggling NetworkManager's autoconnect for a saved network: connecting only
+    // activates a profile, the flag decides whether NM brings it up by itself later.
+    function wifiAutoConnectItem(n) {
+        return '<a class="dropdown-item" href="#" data-wifi-act="autoconnect">' +
+            (n.autoconnect === false ? 'Turn on auto-connect' : 'Turn off auto-connect') + '</a>';
+    }
+    function wifiRowHTML(n, kind) {
+        var limited = kind !== 'connected' && n.usable && n.usable !== 'ok';
+        var right;
+        if (kind === 'connected') {
+            // the ⋮ menu is available here too: IP settings of the LIVE connection is a
+            // real need (the backend re-activates the profile to apply); no Connect item —
+            // it is already up. One-time connections have no stored profile to manage.
+            right = '<button class="btn btn-secondary btn-sm mr-1" type="button" data-wifi-act="disconnect">Disconnect</button>' +
+                (n.profile && !n.onetime
+                    ? '<div class="dropdown">' +
+                      '<button class="btn btn-secondary btn-sm" type="button" data-wifi-act="menu">&#8942;</button>' +
+                      '<div class="dropdown-menu dropdown-menu-right">' +
+                      '<a class="dropdown-item" href="#" data-wifi-act="changepwd">Change password</a>' +
+                      '<a class="dropdown-item" href="#" data-wifi-act="ipconf">IP settings</a>' +
+                      wifiAutoConnectItem(n) +
+                      '<a class="dropdown-item text-danger" href="#" data-wifi-act="forget">Forget</a>' +
+                      '</div></div>'
+                    : '');
+        } else if (kind === 'saved') {
+            right = limited
+                ? '<button class="btn btn-secondary btn-sm" type="button" disabled style="opacity:.5">&#8942;</button>'
+                : '<div class="dropdown">' +
+                  '<button class="btn btn-secondary btn-sm" type="button" data-wifi-act="menu">&#8942;</button>' +
+                  '<div class="dropdown-menu dropdown-menu-right">' +
+                  '<a class="dropdown-item" href="#" data-wifi-act="connect">Connect</a>' +
+                  '<a class="dropdown-item" href="#" data-wifi-act="changepwd">Change password</a>' +
+                  '<a class="dropdown-item" href="#" data-wifi-act="ipconf">IP settings</a>' +
+                  wifiAutoConnectItem(n) +
+                  '<a class="dropdown-item text-danger" href="#" data-wifi-act="forget">Forget</a>' +
+                  '</div></div>';
+        } else {
+            right = '<span class="text-muted">&#8250;</span>';
+        }
+        var badges = '';
+        if ((kind === 'saved' || kind === 'connected') && n.autoconnect === false && !n.onetime) {
+            badges += ' <span class="badge badge-warning wifi-badge" title="Saved, but NetworkManager will not connect to this network automatically">Auto-connect off</span>';
+        }
+        if (limited) {
+            badges += n.usable === 'disabled'
+                ? ' <span class="badge badge-danger wifi-badge" title="' + wifiEsc(wifiLimitedMsg(n)) + '">Unavailable in ' + wifiEsc(wifi.list.country) + '</span>'
+                : ' <span class="badge badge-secondary wifi-badge" title="' + wifiEsc(wifiLimitedMsg(n)) + '">Region required</span>';
+        }
+        // Informational only — a client may legally join both kinds; the badges say why an
+        // access point could not stand there.
+        if (n.indoor_only) {
+            badges += ' <span class="badge badge-info wifi-badge" title="The regulator marks this'
+                   + ' channel indoor-only, and this station is configured as outdoor. Joining is'
+                   + ' fine — the access point cannot use it.">indoor-only</span>';
+        }
+        if (n.dfs) {
+            badges += ' <span class="badge badge-secondary wifi-badge" title="Radar channel'
+                   + ' (DFS). Joining is fine — the access point cannot use it.">DFS</span>';
+        }
+        var meta = [];
+        if (n.band) meta.push(n.band + ' GHz · ch ' + n.chan);
+        meta.push(n.security === 'open' ? 'open' : (n.security_label || n.security));
+        // BSSID always shown: rows are one-per-access-point (a dual-band AP or several
+        // APs sharing an SSID are separate rows), the MAC is what tells them apart
+        if (n.bssid) meta.push(n.bssid);
+        // a hidden-network profile (no BSSID pin) can coexist with a pinned profile of
+        // the same SSID — the mark makes the two rows distinguishable
+        if (n.hidden_profile && kind !== 'available') meta.push('hidden');
+        if (kind === 'connected') {
+            if (n.ip) meta.push(n.ip);
+            if (n.onetime) meta.push('not saved (one-time)');
+        }
+        var lock = n.security !== 'open' ? ' &#128274;' : '';
+        var clickable = kind === 'available' && !limited;
+        return '<div class="wifi-row' + (kind === 'connected' ? ' wifi-connected' : '') +
+            (limited ? ' wifi-limited' : '') + (clickable ? ' wifi-clickable' : '') + '"' +
+            ' data-kind="' + kind + '" data-ssid="' + wifiEsc(n.ssid) + '" data-bssid="' + wifiEsc(n.bssid || '') + '" data-profile="' + wifiEsc(n.profile || '') + '"' +
+            ' data-autoconnect="' + (n.autoconnect === false ? 0 : 1) + '"' +
+            ' data-security="' + wifiEsc(n.security) + '" data-band="' + wifiEsc(n.band || '') + '" data-chan="' + (n.chan || '') + '"' +
+            ' data-limited="' + (limited ? 1 : 0) + '" data-usable="' + wifiEsc(n.usable || 'ok') + '">' +
+            wifiBars(n.signal) +
+            '<div class="wifi-grow"><div class="wifi-name">' + wifiEsc(n.ssid) + lock + badges + '</div>' +
+            '<div class="wifi-meta">' + wifiEsc(meta.join(' · ')) + '</div></div>' + right + '</div>';
+    }
+
+    function wifiRenderList() {
+        var el = $('#wifi-net-list');
+        if (wifi.status && wifi.status.wifi_present === false) {
+            el.html('<div class="text-muted text-center py-4">No WiFi adapter (wlan0) found on this device</div>');
+            return;
+        }
+        if (wifi.scanning) {
+            el.html('<div class="text-muted text-center py-4"><span class="spinner-border spinner-border-sm"></span> Scanning networks…</div>');
+            return;
+        }
+        if (!wifi.list) { el.html(''); return; }
+        if (!wifi.list.radio) {
+            el.html('<div class="text-muted text-center py-4">WiFi is off</div>');
+            return;
+        }
+        var h = '';
+        if (wifi.list.link_stale) {
+            // the radio dropped the link while NetworkManager still points at the network —
+            // the access point holds the only channel the adapter has
+            h += '<div class="small text-warning mb-2">The Wi-Fi network connection was dropped'
+               + ' by the adapter' + (wifi.list.ap_active ? ' — the access point took the radio'
+               : '') + '. Wi-Fi itself is still on; the station reconnects by itself if the'
+               + ' network has auto-connect, otherwise reconnect from the list.</div>';
+        }
+        if (wifi.list.connected.length) {
+            h += '<div class="wifi-grp-title">Connected</div><div class="wifi-list">' +
+                 wifi.list.connected.map(function (n) { return wifiRowHTML(n, 'connected'); }).join('') + '</div>';
+        }
+        if (wifi.list.saved.length) {
+            h += '<div class="wifi-grp-title">Saved networks</div><div class="wifi-list">' +
+                 wifi.list.saved.map(function (n) { return wifiRowHTML(n, 'saved'); }).join('') + '</div>';
+        }
+        h += '<div class="wifi-grp-title">Available networks</div><div class="wifi-list">' +
+             (wifi.list.available.length
+                ? wifi.list.available.map(function (n) { return wifiRowHTML(n, 'available'); }).join('')
+                : (wifi.list.scan_blocked
+                    // прошивка отказывает в скане, пока точка держит 5 ГГц 52–64: refresh
+                    // не поможет — называем канал-виновник и когда сети вернутся
+                    ? '<div class="text-muted small text-center py-3">Scanning is unavailable while the access point runs on ' + wifiEsc(wifi.list.scan_blocked) + ' — networks reappear when the point is off or on 2.4 GHz</div>'
+                    : '<div class="text-muted small text-center py-3">No networks found, please refresh</div>')) +
+             '</div>';
+        h += '<div class="wifi-list mt-2"><div class="wifi-add-row" data-wifi-act="hidden">＋ Add hidden network…</div></div>';
+        el.html(h);
+    }
+
+    function wifiApplyGates() {
+        // list arrives noticeably later than status (it may wait on a scan) — fall back to
+        // status so the AP card isn't gate-locked while the network list is still loading
+        var hasCountry = !!(wifi.list && wifi.list.country) || !!(wifi.status && wifi.status.country);
+        var radioOn = wifi.list ? !!wifi.list.radio : !!(wifi.status && wifi.status.radio);
+        var role = wifi.list ? wifi.list.role : (wifi.status && wifi.status.role);
+        // "AP on" by actual runtime state, not just the autoconnect-derived role: a live AP
+        // whose autoconnect got cleared reads role "client", but must still count as on so the
+        // card stays usable (turn it off) instead of locking under the no-region gate.
+        var apActive = !!(wifi.list && wifi.list.ap_active) || !!(wifi.status && wifi.status.ap_active);
+        var apOn = role === 'ap' || apActive;
+        // The list stays usable with the access point on: the point follows the client onto
+        // its channel, so joining a network is no longer a thing the radio forbids — the
+        // confirmation before joining spells out what happens to the point.
+        $('#wifiApOnNote').toggle(apOn);
+        // Radio off suspends ALL WiFi (NM deactivates a live hotspot too; radio on brings
+        // it back via autoconnect) — the whole AP card locks, nothing to manage meanwhile.
+        // The no-region gate only blocks TURNING THE AP ON: with the AP already running
+        // (e.g. enabled outside the web UI with no region set) the card stays usable so it
+        // can be turned OFF — otherwise the UI has no way out of that state.
+        var noAdapter = !!(wifi.status && wifi.status.wifi_present === false);
+        var apGate = !radioOn || (!hasCountry && !apOn) || noAdapter;
+        $('#wifi-ap-card').toggleClass('wifi-disabled-area', apGate);
+        // natively disable the mode control too — pointer-events on the card is not enough
+        // (e.g. a stale status), and a disabled input can't open the form; the .disabled
+        // class both greys the buttons and makes the buttons plugin ignore the click
+        $('#wifi-ap-switch').prop('disabled', apGate);
+        $('#wifi-ap-auto-switch').prop('disabled', apGate || !!wifi.apSingleIface);
+        // No region while the card itself stays usable (a live AP keeps beaconing on its
+        // old channel, or the form was opened before the region got unset): the form is
+        // VIEW-ONLY — an AP can't be (re)configured without a regulatory domain, so every
+        // field and Save are disabled. Deliberately left usable: the point's own switch
+        // (turning the AP off is the way out of this state) and the password eye (reading
+        // the key of the still-running AP). Applied on state CHANGE only, so the blanket
+        // enable/disable doesn't fight wifiRepopRadio's own Mode/Channel handling.
+        // a live point without a region runs in the world domain — a limited but real
+        // mode, so its profile stays editable (решение по п.80); only a SILENT point
+        // with no region keeps the view-only lock
+        var apFormLock = !hasCountry && !apOn;
+        if (apFormLock !== !!wifi.apFormLock) {
+            wifi.apFormLock = apFormLock;
+            $('#wifi-ap-body').find('input, select, button').not('#wifi-ap-eye')
+                .prop('disabled', apFormLock);
+            if (!apFormLock) wifiRepopRadio();   // restore the band-derived Auto states
+        }
+        $('#wifi-ap-need-region')
+            .text(apOn
+                ? 'No WiFi region is selected — the access point runs in the limited '
+                  + 'world-safe mode (2.4 GHz ch 1–11). Select a region above for the full choice.'
+                : '⚠ Access Point mode requires a WiFi region (country) — select it above.')
+            .toggle(!hasCountry && radioOn);
+        // Only the radio being off (or no adapter) blocks a rescan: the access point lives
+        // on its own interface, so scanning works while it runs — same rule as the
+        // 'wifi list' handler, or the button would flip with whichever event came last.
+        $('#wifi-rescan-button').prop('disabled', !radioOn || noAdapter);
+        $('#wifi-region-hint').toggle(!hasCountry);
+    }
+
+    function wifiRenderCountries(countries) {
+        var sel = $('#wifi-country');
+        var current = (wifi.status && wifi.status.country) || '';
+        var html = countries.map(function (c) {
+            return '<option value="' + c.code + '" data-tokens="' + c.code + '"' +
+                (c.code === current ? ' selected' : '') + '>' + wifiEsc(c.name) + ' (' + c.code + ')</option>';
+        }).join('');
+        sel.html(html);
+        if (!current) sel.val([]);
+        // selectpicker init below may fire changed.bs.select for the preselected option —
+        // record it as the programmatic value so the change handler doesn't emit for it
+        wifi.syncedCountry = current;
+        wifi.countriesLoaded = true;
+        if (sel.data('selectpicker')) sel.selectpicker('destroy');
+        sel.selectpicker({liveSearch: true, liveSearchStyle: 'startsWith', size: 8, width: '100%',
+                          title: 'Choose a WiFi region…'});
+    }
+
+    function wifiSyncCountrySelect() {
+        if (!wifi.countriesLoaded) return;
+        wifi.updating = true;
+        var current = (wifi.status && wifi.status.country) || '';
+        // bootstrap-select can fire changed.bs.select asynchronously (after wifi.updating is
+        // reset), so also remember the value we set programmatically and ignore a change equal
+        // to it — otherwise a region sync (e.g. right after Import) re-emits "wifi set country".
+        wifi.syncedCountry = current;
+        $('#wifi-country').val(current || []).selectpicker('refresh');
+        wifi.updating = false;
+    }
+
+    // Security choices come from the backend: an adapter whose supplicant has no SAE cannot
+    // put WPA3 next to WPA2 on one access point, so offering "WPA2 + WPA3" there would promise
+    // something the radio never delivers (it beacons plain WPA2). Rebuilt on every render
+    // because a fresh 'wifi status' replaces the list.
+    var WIFI_SEC_LABEL = {
+        mixed: 'WPA2 + WPA3 (recommended)',
+        wpa3: 'WPA3-SAE',
+        wpa2: 'WPA2-PSK',
+        open: 'Open (no password)'
+    };
+    function wifiRepopApSecurity(selected) {
+        var kinds = (wifi.status && wifi.status.ap_security_kinds) || ['mixed', 'wpa3', 'wpa2', 'open'];
+        var sel = $('#wifi-ap-sec');
+        if (kinds.indexOf(selected) < 0) selected = kinds[0];
+        sel.html(kinds.map(function (k) {
+            return '<option value="' + k + '"' + (k === selected ? ' selected' : '') + '>' +
+                   wifiEsc(WIFI_SEC_LABEL[k] || k) + '</option>';
+        }).join(''));
+        return selected;
+    }
+
+    function wifiRenderApForm(cfg) {
+        wifi.updating = true;
+        try {
+            wifiRenderApFormBody(cfg);
+        } catch (e) {
+            // 89-повтор: исключение посреди рендера оставляло уже показанные строки без
+            // финального вердикта (хор) и глушило форму зависшим updating — падать молча
+            // нельзя, а вердикт и разблокировка обязаны случиться при любом исходе
+            if (window.console && console.error) console.error('AP form render failed:', e);
+        } finally {
+            wifiApOneVoice();
+            wifi.updating = false;
+            wifi.apDirty = false;   // the form now mirrors the stored config — nothing to lose
+        }
+    }
+
+    function wifiRenderApFormBody(cfg) {
+        $('#wifi-ap-ssid').val(cfg.ssid);
+        wifi.apStoredSecurity = wifiRepopApSecurity(cfg.security);
+        $('#wifi-ap-sec').val(wifi.apStoredSecurity);
+        // Show the stored key only under the CSS masking (type=text — invisible to the
+        // browser's password manager, so no save prompts). With the type=password fallback
+        // the field stays empty (•••• placeholder); an empty submit keeps the stored key.
+        wifi.apHasPassword = !!cfg.has_password;
+        if (wifiPwdCssMask) {
+            $('#wifi-ap-pwd').val(cfg.password || '').attr('placeholder', 'Enter AP password');
+        } else {
+            $('#wifi-ap-pwd').val('').attr('placeholder',
+                cfg.has_password ? '••••••••' : 'Enter AP password');
+        }
+        $('#wifi-ap-hidden').prop('checked', !!cfg.hidden);
+        $('#wifi-ap-band').val(cfg.band || 'all');
+        $('#wifi-ap-ip').val(cfg.ip);
+        $('#wifi-ap-mask').html(wifiMaskOptions(cfg.prefix || 24));
+        // the adapter has one channel: while a client network is up, the point stands on ITS
+        // channel, and the picker above only says where the point goes once that link ends
+        // Where the beacon really is. The form holds intent; these two part company when the
+        // client link owns the channel or the rescue path picked one, and until now nothing in
+        // the dialog said so.
+        var air = cfg.air_channel
+            ? (cfg.air_band ? cfg.air_band + ' GHz ch ' + cfg.air_channel : 'ch ' + cfg.air_channel)
+            : '';
+        var askedFor = +(cfg.channel || 0);
+        var mismatch = air && askedFor && +cfg.air_channel !== askedFor;
+        $('#wifi-ap-air')
+            .text(!air ? '' : mismatch
+                ? 'The access point is on ' + air + ', not on the channel selected here.'
+                : 'The access point is on ' + air + '.')
+            .toggleClass('text-warning', !!mismatch)
+            .toggleClass('text-muted', !mismatch)
+            .toggle(!!air);
+        // A channel legal in one region is not in another, and changing the region does not
+        // move a running point.
+        $('#wifi-ap-illegal')
+            .text(cfg.channel_illegal
+                ? (+(cfg.channel || 0) === 0
+                    // канал выбирал не оператор (Auto): совет «выберите Auto» абсурден —
+                    // говорим честно, что поднялась вопреки политике и как из этого выйти
+                    ? 'The point came up on a channel this station\u2019s policy does not allow ('
+                      + cfg.channel_illegal + '). Switch the point off, set an allowed channel '
+                      + 'explicitly, or adjust the region / outdoor setting.'
+                    : 'Not allowed here: ' + cfg.channel_illegal
+                      + '. Pick another channel or Auto and save.')
+                : '')
+            .toggle(!!cfg.channel_illegal);
+        $('#wifi-ap-channel-note')
+            .text(cfg.channel_active ? 'The access point currently runs on ' + cfg.channel_active
+                                       + ' — it follows the channel of the connected Wi-Fi'
+                                       + ' network. Your choice applies when nothing is connected.'
+                                     : '')
+            .toggle(!!cfg.channel_active);
+        $('#wifi-ap-yielded')
+            .text(cfg.yielded ? 'The access point is not running: ' + cfg.yielded + '.' : '')
+            .toggle(!!cfg.yielded);
+        $('#wifi-ap-ip-moved')
+            .text(cfg.ip_active ? 'This subnet is in use by a network the station is connected'
+                                  + ' to, so the access point currently runs on ' + cfg.ip_active
+                                  + '. It returns to the address above once the subnet is free.' : '')
+            .toggle(!!cfg.ip_active);
+        $('#wifi-ap-pwd-field').toggle(cfg.security !== 'open');
+        // two independent switches + the on-air indicator in the card header
+        wifi.apStoredEnabled = !!cfg.enabled;
+        wifi.apStoredAuto = !!cfg.auto;
+        wifi.apConfigured = !!cfg.configured;
+        wifi.apOnAir = !!cfg.on_air;
+        wifi.apRaisedByAuto = !!cfg.raised_by_auto;
+        wifi.apSingleIface = !!cfg.single_iface;
+        wifi.apPendingAuto = false;   // the form mirrors the stored config again
+        // single-interface legacy (the point runs on wlan0 itself): Auto-start cannot work
+        // there — the switch is disabled, the note below says which mode this is
+        $('#wifi-ap-auto-switch').prop('disabled', !!cfg.single_iface);
+        wifiApSyncSwitches();
+        // the on-air lamp never disappears: green while the beacon is up (with where it
+        // is), quiet outlined "Off air" while it is not — a state, not a decoration
+        var lamp = $('#wifi-ap-onair');
+        if (cfg.on_air) {
+            var airAt = cfg.air_channel
+                ? ' · ' + (cfg.air_band ? cfg.air_band + ' GHz ch ' + cfg.air_channel
+                                             : 'ch ' + cfg.air_channel)
+                : '';
+            lamp.attr('class', 'badge badge-success wifi-badge')
+                .text('Online' + airAt)
+                .attr('title', 'The access point is broadcasting');
+        } else {
+            lamp.attr('class', 'badge wifi-badge wifi-offair')
+                .text('Offline')
+                .attr('title', 'The access point is not broadcasting');
+        }
+        $('#wifi-ap-byauto').toggle(!!(cfg.on_air && cfg.raised_by_auto));
+        // a live point that will not come back by itself (autoconnect off — the watchdog's
+        // point, or NM cleared the flag after failures): say so instead of hiding it in
+        // the switch position (п.81 made the switch follow the air)
+        $('#wifi-ap-noautoconnect').toggle(!!(cfg.on_air && cfg.configured && !cfg.autoconnect));
+        wifi.apStoredChannel = +(cfg.channel || 0);   // drives the not-available flag below
+        wifi.apStoredBand = cfg.band || 'all';
+        wifiRepopRadio();
+        if (cfg.channel) $('#wifi-ap-channel').val(String(cfg.channel));
+        // persistent start-failure line (survives the toast and a dialog reopen)
+        $('#wifi-ap-last-err').text(cfg.last_error ? 'Access point failed to start: ' + cfg.last_error : '')
+            .toggle(!!cfg.last_error);
+    }
+
+    function wifiGatherApForm(enabled) {
+        return {
+            enabled: enabled,
+            ssid: $('#wifi-ap-ssid').val(),
+            security: $('#wifi-ap-sec').val(),
+            password: $('#wifi-ap-pwd').val(),
+            hidden: $('#wifi-ap-hidden').prop('checked'),
+            band: $('#wifi-ap-band').val(),
+            channel: +($('#wifi-ap-channel').val() || 0),
+            ip: $('#wifi-ap-ip').val().trim(),
+            prefix: +$('#wifi-ap-mask').val()
+        };
+    }
+
+    function wifiFindNet(profile) {
+        if (!wifi.list || !profile) return null;
+        var all = (wifi.list.connected || []).concat(wifi.list.saved || [], wifi.list.available || []);
+        for (var i = 0; i < all.length; i++) if (all[i].profile === profile) return all[i];
+        return null;
+    }
+
+    // ---- connect / hidden / change password / ip settings dialog ----
+    function wifiOpenConnect(opts) {
+        wifi.cm = opts;
+        var isHidden = opts.mode === 'hidden';
+        var isPwd = opts.mode === 'changepwd';
+        var isIp = opts.mode === 'ipconf';
+        wifiPwdSetMasked($('#wifi-cm-pwd').val(''), true);
+        $('#wifi-cm-eye').html(WIFI_EYEOFF);
+        wifiShowPwdErr('');
+        $('#wifi-cm-ssid').val('');
+        $('#wifi-cm-ssid-visible').hide();
+        $('#wifi-cm-ip, #wifi-cm-gw, #wifi-cm-dns').val('').css('border-color', '');
+        $('#wifi-cm-dns2').val('');
+        $('#wifi-cm-dns2-err').hide();
+        $('#wifi-cm-autodns').show();   // default mode is Automatic
+        $('#wifi-cm-mask').html(wifiMaskOptions(24));
+        $('#wifi-cm-ip-err').hide();
+        $('#wifi-cm-ipmode label').removeClass('active');
+        $('#wifi-cm-ipmode input[value="auto"]').prop('checked', true).closest('label').addClass('active');
+        $('#wifi-cm-static').hide();
+        $('#wifi-cm-save').prop('checked', true);
+        $('#wifi-cm-ssid-field').toggle(isHidden);
+        $('#wifi-cm-sec-field').toggle(isHidden);
+        $('#wifiHiddenNote').toggle(isHidden && !(wifi.status && wifi.status.country));
+        $('#wifi-cm-ipblock').toggle(!isPwd);
+        $('#wifi-cm-save').closest('.custom-control').toggle(!isPwd && !isIp);
+        $('#wifi-cm-pwd-field').toggle(!isIp && (isHidden || isPwd || opts.security !== 'open'));
+        wifiPwdSetHint(isIp ? '' : (isHidden ? $('#wifi-cm-sec').val() : opts.security));
+        if (isHidden) {
+            $('#wifi-cm-title').text('Add hidden network');
+            $('#wifi-cm-meta').text('Enter the network name, security and password.');
+            $('#wifi-cm-connect').text('Connect');
+        } else if (isPwd) {
+            $('#wifi-cm-title').text('Change password — “' + opts.ssid + '”');
+            $('#wifi-cm-meta').text('');
+            $('#wifi-cm-connect').text('Save');
+        } else if (isIp) {
+            $('#wifi-cm-title').text('IP settings — “' + opts.ssid + '”');
+            $('#wifi-cm-meta').text('Automatic (DHCP) or a manual static address for this saved network.');
+            $('#wifi-cm-connect').text('Save');
+            // pre-fill with the profile's CURRENT configuration (the reset above assumed DHCP)
+            var net = wifiFindNet(opts.profile), ipc = net && net.ipconf;
+            if (ipc && ipc.method !== 'manual' && ipc.dns) {
+                $('#wifi-cm-dns2').val(ipc.dns);   // DHCP with a DNS override stored
+            }
+            if (ipc && ipc.method === 'manual') {
+                $('#wifi-cm-ipmode label').removeClass('active');
+                $('#wifi-cm-ipmode input[value="manual"]').prop('checked', true).closest('label').addClass('active');
+                $('#wifi-cm-static').show();
+                $('#wifi-cm-autodns').hide();
+                $('#wifi-cm-ip').val(ipc.ip || '');
+                $('#wifi-cm-mask').html(wifiMaskOptions(ipc.prefix || 24));
+                $('#wifi-cm-gw').val(ipc.gateway || '');
+                $('#wifi-cm-dns').val(ipc.dns || '');
+            }
+        } else {
+            $('#wifi-cm-title').text('Connect to “' + opts.ssid + '”');
+            var open = opts.security === 'open';
+            $('#wifi-cm-meta').text('Security: ' + (open ? 'open' : (opts.securityLabel || opts.security)) +
+                (opts.band ? ' · ' + opts.band + ' GHz · channel ' + opts.chan : '') +
+                (opts.bssid ? ' · ' + opts.bssid : ''));
+            $('#wifi-cm-connect').text('Connect');
+        }
+        $('#wifiConnectModal').modal();
+    }
+
+    function wifiGatherIpconf() {
+        var mode = $('#wifi-cm-ipmode input:checked').val();
+        if (mode !== 'manual') return {method: 'auto', dns: $('#wifi-cm-dns2').val().trim()};
+        return {
+            method: 'manual',
+            ip: $('#wifi-cm-ip').val().trim(),
+            prefix: +$('#wifi-cm-mask').val(),
+            gateway: $('#wifi-cm-gw').val().trim(),
+            dns: $('#wifi-cm-dns').val().trim()
+        };
+    }
+
+    function wifiValidateAutoDns(ipconf) {
+        // DHCP + DNS override: only the server list needs checking
+        if (ipconf.method === 'manual' || !ipconf.dns) return true;
+        var bad = null;
+        ipconf.dns.split(',').forEach(function (srv) {
+            srv = srv.trim();
+            if (srv && !/^((25[0-5]|2[0-4]\d|1?\d?\d)\.){3}(25[0-5]|2[0-4]\d|1?\d?\d)$/.test(srv)) bad = srv;
+        });
+        $('#wifi-cm-dns2-err').text(bad ? 'Invalid DNS server address: ' + bad : '').toggle(!!bad);
+        return !bad;
+    }
+    // Joining a network takes the radio away from the access point, so an operator working
+    // THROUGH that hotspot loses this page mid-request. Warn once, in both connect paths, and
+    // say how they get back in — the hotspot returns by itself if the network cannot be joined.
+    // channel of the network being joined, taken from the list the backend already sent
+    function wifiTargetChan(ssid, bssid) {
+        var all = (wifi.list ? (wifi.list.connected || []).concat(wifi.list.saved || [],
+                                                                 wifi.list.available || []) : []);
+        for (var i = 0; i < all.length; i++) {
+            var n = all[i];
+            var hit = bssid ? (n.bssid || '').toUpperCase() === String(bssid).toUpperCase()
+                            : n.ssid === ssid;
+            if (hit && n.chan && n.band) return {band: n.band, chan: n.chan};
+        }
+        return null;
+    }
+
+    function wifiConfirmLosingHotspot(ssid, bssid) {
+        if (!(wifi.status && (wifi.status.role === 'ap' || wifi.status.ap_active))) return true;
+        var t = wifiTargetChan(ssid, bssid);
+        // the adapter holds one channel for both roles, so joining moves the access point onto
+        // the network's channel — or, where it may not run there, switches it off
+        if (t && wifiApChannels(t.band).indexOf(t.chan) >= 0) {
+            return confirm('The access point will move to channel ' + t.chan + ' to join "' +
+                ssid + '" — the adapter has a single channel for both.\n\n' +
+                'Devices using the access point will have to reconnect, so this page may pause ' +
+                'briefly if it is one of them. If the network cannot be joined, the access ' +
+                'point returns to its own channel.');
+        }
+        return confirm('The access point will be switched off to join "' + ssid + '"' +
+            (t ? ', which is on channel ' + t.chan + ' — the access point cannot run there' : '') +
+            '.\n\nIf you are connected to the station through it, this page will lose its ' +
+            'connection. On success the station is reachable on the new network; if it ' +
+            'cannot join, the access point comes back automatically.');
+    }
+
+    function wifiDoConnect() {
+        var m = wifi.cm;
+        if (m.mode === 'changepwd') {
+            var pw = $('#wifi-cm-pwd').val();
+            var pe = wifiValidatePwd(m.security, pw);
+            if (pe) { wifiShowPwdErr(pe); return; }
+            socket.emit('wifi change password', {profile: m.profile, password: pw});
+            $('#wifiConnectModal').modal('hide');
+            wifiSetBusy(true, 'Saving password…', 'Password saved');
+            return;
+        }
+        var ipconf = wifiGatherIpconf();
+        if (ipconf.method === 'manual' &&
+            !wifiValidateStatic({ip: 'wifi-cm-ip', mask: 'wifi-cm-mask', gw: 'wifi-cm-gw', dns: 'wifi-cm-dns', err: 'wifi-cm-ip-err', softOccupied: true})) {
+            return;
+        }
+        if (!wifiValidateAutoDns(ipconf)) return;
+        if (m.mode === 'ipconf') {
+            socket.emit('wifi set ipconf', {profile: m.profile, ipconf: ipconf});
+            $('#wifiConnectModal').modal('hide');
+            wifiSetBusy(true, 'Applying IP settings…', 'IP settings applied');
+            return;
+        }
+        var msg = {remember: $('#wifi-cm-save').prop('checked'), ipconf: ipconf};
+        if (m.mode === 'hidden') {
+            msg.ssid = $('#wifi-cm-ssid').val().trim();
+            if (!msg.ssid) { wifiToast('Enter the network name (SSID)', true); return; }
+            msg.security = $('#wifi-cm-sec').val();
+            msg.hidden = true;
+            if (msg.security === 'wep') msg.security = 'wep';
+        } else {
+            msg.ssid = m.ssid;
+            // pin the connection to the exact access point the user clicked — rows are
+            // one-per-BSSID, and a stationary station must not roam to a namesake AP
+            msg.bssid = m.bssid || null;
+            msg.security = null;   // resolved from the scan on the backend
+        }
+        msg.password = $('#wifi-cm-pwd').val();
+        // secured network needs a valid key (open sends none). m.security holds the network's
+        // security even when msg.security is null (the backend resolves it from the scan).
+        var sec = (m.mode === 'hidden') ? msg.security : m.security;
+        var pwdErr = wifiValidatePwd(sec, msg.password);
+        if (pwdErr) { wifiShowPwdErr(pwdErr); return; }
+        if (!wifiConfirmLosingHotspot(msg.ssid, msg.bssid)) return;
+        socket.emit('wifi connect', msg);
+        $('#wifiConnectModal').modal('hide');
+        wifiSetBusy(true, 'Connecting to ' + msg.ssid + '…', '', 'result');
+    }
+
+    socket.on('wifi hidden probe', function (res) {
+        // an answer for an SSID the operator has since edited is stale — drop it; the probe
+        // is a real scan of the air and takes seconds, so it is compared against what the
+        // field holds NOW, not only against what was asked
+        if (!res || res.ssid !== wifi.probeSsid
+            || res.ssid !== $('#wifi-cm-ssid').val().trim()) return;
+        if (!res.found) { $('#wifi-cm-sec-detected').hide(); return; }
+        if (res.security) {
+            $('#wifi-cm-sec').val(res.security);
+            $('#wifi-cm-pwd-field').toggle(res.security !== 'open');
+            wifiPwdSetHint(res.security);
+        }
+        $('#wifi-cm-sec-detected')
+            .text('Detected: ' + (res.security_label || res.security) +
+                  (res.chan ? ', ' + res.band + ' GHz ch ' + res.chan : '') +
+                  '. Change it if the network says otherwise.')
+            .show();
+    });
+
+    // ---- socket handlers ----
+    socket.on('wifi status', function (status) {
+        wifi.status = status;
+        if (status.capability && !status.capability.nm_running) {
+            // remember what WE hid, so NetworkManager coming back can restore exactly it
+            // (a blanket .show() would also reveal notes that are hidden by design)
+            $('#wifiModal .modal-body').children(':visible').addClass('wifi-nm-hid').hide();
+            // Export/Import live in the footer (outside .modal-body) — hide them too:
+            // there is nothing to back up or restore without NetworkManager. Close stays.
+            $('#wifi-backup-button, #wifi-import-button').hide();
+            if (!$('#wifi-no-nm').length) {
+                $('#wifiModal .modal-body').append('<div id="wifi-no-nm" class="alert alert-warning"></div>');
+            }
+            // п.96: NM, остановленный чужим скриптом, — не «не та ОС»; и баннер снимается
+            // сам, когда NM возвращается (см. ветку восстановления ниже)
+            $('#wifi-no-nm').text(status.capability.nm
+                ? 'NetworkManager is not running — Wi-Fi settings will return when it is back. The interfaces below are read from the kernel.'
+                : 'WiFi settings are not available on this OS (NetworkManager is required) — use Debian 12 or 13.');
+            // every status event re-runs the children().hide() above, which also hides the
+            // alert appended on a PREVIOUS event (reopening the dialog requests a fresh
+            // status) — re-show it explicitly or the second open ends up an empty dialog
+            $('#wifi-no-nm').show();
+            // the interface table still has data without NM (kernel-sourced fallback on
+            // the backend) — render and re-show it; only the connection name is missing
+            wifiRenderIfaces();
+            $('#wifi-ifaces').show();
+            return;
+        }
+        // NM вернулся после остановки — вернуть спрятанное и убрать баннер (п.96)
+        if ($('#wifi-no-nm').length && $('#wifi-no-nm').is(':visible')) {
+            $('#wifi-no-nm').hide();
+            $('#wifiModal .modal-body').children('.wifi-nm-hid').removeClass('wifi-nm-hid').show();
+            $('#wifi-backup-button, #wifi-import-button').show();
+        }
+        wifiRenderIfaces();
+        // wifi_manager.conf беды (кодировка/нечитаемость): настройки падают в умолчания,
+        // строка в журнале этого никому не сказала — говорим прямо в диалоге
+        $('#wifi-conf-warn')
+            .text(status.conf_problem
+                ? '\u26a0 Station settings file (wifi_manager.conf): ' + status.conf_problem
+                : '')
+            .toggle(!!status.conf_problem);
+        wifi.updating = true;
+        $('#wifi-radio-switch').prop('checked', !!status.radio);
+        wifi.updating = false;
+        wifiSyncCountrySelect();
+        wifiRenderCountryTemp(status);
+        // adapter without 5 GHz (e.g. RPi Zero 2W): hide the 5 GHz AP band option
+        if (status.bands && status.bands.indexOf('5') < 0) {
+            $('#wifi-ap-band option[value="5"]').remove();
+        }
+        wifiRepopRadio();
+        wifiApplyGates();
+    });
+
+    socket.on('wifi countries', function (countries) {
+        wifiRenderCountries(countries);
+    });
+
+    socket.on('wifi list', function (list) {
+        wifiBusyDoneIf('list');   // a background-rescan list must NOT unlock a running connect
+        wifi.scanning = false;
+        wifi.list = list;
+        wifiRenderList();
+        wifiApplyGates();
+        // the client link state feeds the band/channel lock (п.52) and the verdict line
+        wifiApRadioLock();
+        wifiApOneVoice();
+        // Rescan works with the point up: it runs on its own interface, the client one can
+        // still scan. Only the radio being off leaves nothing to scan with.
+        $('#wifi-rescan-button').prop('disabled', !list.radio);
+    });
+
+    socket.on('ap config', function (cfg) {
+        wifiBusyDoneIf('ap');
+        wifiRenderApForm(cfg);
+        wifiApplyGates();
+    });
+
+    socket.on('wifi error', function (err) {
+        wifi.busyDone = '';           // it failed — suppress any pending success confirmation
+        wifiSetBusy(false);
+        if (wifi.scanning) { wifi.scanning = false; wifiRenderList(); }
+        wifiToast((err.op ? err.op + ': ' : '') + err.message, true);
+    });
+
+    socket.on('wifi connect status', function (st) {
+        // progress updates during a blocking op: refresh the persistent spinner message
+        var map = {
+            connecting: 'Connecting to ' + (st.detail || '') + '…',
+            reconnecting: 'Re-connecting…',
+            connected: 'Connected',
+            starting: 'Starting access point…',
+            disconnected: 'Link dropped — trying an allowed channel…'
+        };
+        var m = map[st.stage] || st.detail || st.stage;
+        if (wifi.busy) wifiSetBusy(true, m, wifi.busyDone, wifi.busyTerminal);
+        else wifiToast(m, st.stage === 'failed' || st.stage === 'disconnected');
+    });
+
+    socket.on('wifi connect result', function (res) {
+        wifiSetBusy(false);
+        if (res.ok) wifiToast('Connected');
+        else wifiToast(res.message || 'Connection failed', true);
+    });
+
+    socket.on('wifi country result', function () {
+        wifiBusyDoneIf('country');   // region apply finished; fresh status/list arrived just before
+    });
+
+    socket.on('wifi backup', function (data) {
+        var blob = new Blob([JSON.stringify(data, null, 2)], {type: 'application/json'});
+        var a = document.createElement('a');
+        a.href = URL.createObjectURL(blob);
+        // name the file after the station so exports from several stations don't collide;
+        // sanitize: the station name may come from a mount point / hostname / IP
+        var station = String($('#wifi-backup-button').data('station') || 'rtkbase')
+            .replace(/[^\w.-]+/g, '_').replace(/^[_.]+|[_.]+$/g, '') || 'rtkbase';
+        a.download = station + '_wifi_profiles.json';
+        a.click();
+        URL.revokeObjectURL(a.href);
+    });
+
+    socket.on('wifi restore result', function (res) {
+        wifiSetBusy(false);   // terminal for the 'restore' busy state
+        if (res.ok) wifiToast('Imported ' + res.count + ' WiFi profile(s)');
+        else wifiToast(res.message || 'Import failed', true);
+    });
+
+    // ---- UI events ----
+    $('#manage-wifi-button').on('click', function () {
+        // нажатый «глазик» не переживает переоткрытие: пароли всегда открываются скрытыми
+        wifiPwdSetMasked($('#wifi-ap-pwd'), true); $('#wifi-ap-eye').html(WIFI_EYE);
+        wifiPwdSetMasked($('#wifi-cm-pwd'), true); $('#wifi-cm-eye').html(WIFI_EYE);
+        // backend re-raises the radio on "get wifi status" when it went down on its own
+        // (boot/rfkill); an OFF chosen via the dialog's radio toggle is kept
+        socket.emit('get wifi status');
+        if (!wifi.countriesLoaded) socket.emit('get wifi countries');
+        socket.emit('get wifi list');   // backend forces a fresh scan (may take a few seconds)
+        socket.emit('get ap config');
+        // the first render above waits behind the opening scan — until it lands, the
+        // dialog must show the last known truth, not whatever the DOM woke up with
+        // (a reloaded page restores checkbox positions, and markup defaults are blind)
+        wifiApSyncSwitches();
+        wifi.scanning = true; wifiRenderList();   // list shows "Scanning…" while the initial scan runs
+        $('#wifiModal').modal();
+        wifiStartAutoScan();
+    });
+    $('#wifiModal').on('hidden.bs.modal', function () {
+        clearInterval(wifiAutoScan);
+        wifi.busyDone = '';
+        wifiSetBusy(false);          // popup closed — the op continues server-side; drop the toast
+        $('#wifi-toast').hide();
+    });
+
+    $('#wifi-rescan-button').on('click', function () {
+        // the list area shows the "Scanning…" placeholder — no separate status toast (would duplicate)
+        wifi.scanning = true; wifiRenderList();
+        socket.emit('wifi rescan');
+    });
+
+    $('#wifi-radio-switch').on('change', function () {
+        if (wifi.updating) return;
+        var on = $(this).prop('checked');
+        // power-on triggers a fresh scan on the backend; show the placeholder meanwhile
+        wifi.scanning = on; wifiRenderList();
+        socket.emit('wifi radio', {on: on});
+    });
+
+    $('#wifi-country').on('changed.bs.select', function () {
+        if (wifi.updating) return;
+        var code = $(this).val();
+        // Skip only what the code itself put into the picker (wifi.syncedCountry): every
+        // programmatic sync records its value there, so anything else IS a user action.
+        // Deliberately NOT compared against wifi.status.country — that snapshot can be
+        // stale while a region change is applying, and matching it would silently swallow
+        // a legitimate change (the picker then "snaps back" once the real status lands).
+        if (!code || code === wifi.syncedCountry) return;
+        var connected = wifi.list && wifi.list.connected.length > 0;
+        var apOn = wifi.list && (wifi.list.role === 'ap' || wifi.list.ap_active);
+        if (connected && !window.confirm('Changing the WiFi region re-applies the regulatory domain to the adapter; the current connection may briefly drop and reconnect. Continue?')) {
+            wifiSyncCountrySelect();
+            return;
+        }
+        if (apOn && !window.confirm('Changing the WiFi region may require restarting the hotspot on a valid channel; access point clients will briefly disconnect. Continue?')) {
+            wifiSyncCountrySelect();
+            return;
+        }
+        // own terminal: a 'wifi list' from an unrelated scan (e.g. the popup-open scan
+        // finishing) must not unlock the dialog while the region is still being applied
+        wifiSetBusy(true, 'Applying WiFi region ' + code + '…', 'WiFi region applied', 'country');
+        socket.emit('wifi set country', {country: code});
+    });
+
+    function wifiRenderCountryTemp(status) {
+        // The shown region can be a visitor: joining a network imports the beacon's
+        // country, support may set one by hand. It works right now but is not recorded
+        // for boots, so a reboot returns the station to the recorded region — say so and
+        // offer to keep the current one. The picker itself cannot re-pick the same value
+        // (same-value changes are swallowed by design), which is exactly why the button
+        // exists.
+        var show = !!(status.country_temporary && status.country);
+        if (show) {
+            $('#wifi-country-temp-text').text('Region ' + status.country
+                + ' was picked up from a connected network or set automatically and lasts '
+                + 'until reboot — the station then returns to '
+                + (status.country_stored ? 'region ' + status.country_stored : 'no region') + '.');
+            $('#wifi-country-keep').text('Keep ' + status.country);
+        }
+        // no d-flex on the container: bootstrap's !important display would win over
+        // hide(), so the flex display is set here
+        $('#wifi-country-temp').css('display', show ? 'flex' : 'none');
+    }
+
+    $('#wifi-country-keep').on('click', function () {
+        var code = wifi.status && wifi.status.country;
+        if (!code) return;
+        // same busy terminal as the picker: applying an already-active domain is quick,
+        // but the cmdline write must finish before the dialog unlocks
+        wifiSetBusy(true, 'Saving WiFi region ' + code + '…', 'WiFi region saved', 'country');
+        socket.emit('wifi set country', {country: code});
+    });
+
+    // network list actions (delegated: the list is re-rendered on every update)
+    $('#wifi-net-list').on('click', '[data-wifi-act]', function (e) {
+        e.preventDefault();
+        e.stopPropagation();
+        var act = $(this).data('wifi-act');
+        var row = $(this).closest('.wifi-row');
+        if (act === 'menu') {
+            var menu = $(this).siblings('.dropdown-menu');
+            $('#wifi-net-list .dropdown-menu').not(menu).removeClass('show');
+            menu.toggleClass('show');
+            return;
+        }
+        $('#wifi-net-list .dropdown-menu').removeClass('show');
+        if (act === 'hidden') { wifiOpenConnect({mode: 'hidden'}); return; }
+        if (act === 'disconnect') { socket.emit('wifi disconnect'); wifiSetBusy(true, 'Disconnecting…', 'Disconnected'); return; }
+        if (act === 'forget') {
+            socket.emit('wifi forget', {profile: row.data('profile')});
+            wifiSetBusy(true, 'Forgetting network…', 'Network forgotten');
+            return;
+        }
+        if (act === 'changepwd') {
+            wifiOpenConnect({mode: 'changepwd', ssid: row.data('ssid'), profile: row.data('profile'),
+                             security: row.data('security')});
+            return;
+        }
+        if (act === 'autoconnect') {
+            var turnOn = !+row.data('autoconnect');
+            socket.emit('wifi set autoconnect', {profile: row.data('profile'), on: turnOn});
+            wifiSetBusy(true, turnOn ? 'Enabling auto-connect…' : 'Disabling auto-connect…',
+                        turnOn ? 'Auto-connect enabled' : 'Auto-connect disabled');
+            return;
+        }
+        if (act === 'ipconf') {
+            wifiOpenConnect({mode: 'ipconf', ssid: row.data('ssid'), profile: row.data('profile')});
+            return;
+        }
+        if (act === 'connect') {
+            if (row.data('limited')) { wifiToast(wifiLimitedMsg({usable: row.data('usable')}), true); return; }
+            var prof = row.data('profile');
+            if (prof) {   // saved network: password is stored — just re-activate, don't prompt
+                if (!wifiConfirmLosingHotspot(row.data('ssid'), row.data('bssid'))) return;
+                // the row's BSSID rides along: a pre-BSSID profile gets pinned to this AP
+                socket.emit('wifi activate', {profile: prof, bssid: row.data('bssid') || null});
+                wifiSetBusy(true, 'Connecting to ' + row.data('ssid') + '…', '', 'result');
+            } else {      // fallback: no profile → treat as a fresh connection
+                wifiOpenConnect({mode: 'connect', ssid: row.data('ssid'), bssid: row.data('bssid'),
+                                 security: row.data('security'),
+                                 band: row.data('band'), chan: row.data('chan')});
+            }
+            return;
+        }
+    });
+
+    $('#wifi-net-list').on('click', '.wifi-row[data-kind="available"]', function () {
+        var row = $(this);
+        if (row.data('limited')) { wifiToast(wifiLimitedMsg({usable: row.data('usable')}), true); return; }
+        wifiOpenConnect({mode: 'connect', ssid: row.data('ssid'), bssid: row.data('bssid'),
+                         security: row.data('security'),
+                         band: row.data('band'), chan: row.data('chan')});
+    });
+
+    $(document).on('click', function (e) {
+        if (!$(e.target).closest('#wifi-net-list .dropdown').length) {
+            $('#wifi-net-list .dropdown-menu').removeClass('show');
+        }
+    });
+
+    // connect dialog events
+    $('#wifi-cm-connect').on('click', wifiDoConnect);
+    // Bootstrap Icons eye / eye-slash — the same glyphs the settings-page password fields
+    // (Caster password etc.) use, so the WiFi show/hide button matches them. EYE = revealed,
+    // EYEOFF (slash) = masked.
+    var WIFI_EYE = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-eye" viewBox="0 0 16 16"><path d="M16 8s-3-5.5-8-5.5S0 8 0 8s3 5.5 8 5.5S16 8 16 8zM1.173 8a13.133 13.133 0 0 1 1.66-2.043C4.12 4.668 5.88 3.5 8 3.5c2.12 0 3.879 1.168 5.168 2.457A13.133 13.133 0 0 1 14.828 8c-.058.087-.122.183-.195.288-.335.48-.83 1.12-1.465 1.755C11.879 11.332 10.119 12.5 8 12.5c-2.12 0-3.879-1.168-5.168-2.457A13.134 13.134 0 0 1 1.172 8z"/><path d="M8 5.5a2.5 2.5 0 1 0 0 5 2.5 2.5 0 0 0 0-5zM4.5 8a3.5 3.5 0 1 1 7 0 3.5 3.5 0 0 1-7 0z"/></svg>';
+    var WIFI_EYEOFF = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-eye-slash" viewBox="0 0 16 16"><path d="M13.359 11.238C15.06 9.72 16 8 16 8s-3-5.5-8-5.5a7.028 7.028 0 0 0-2.79.588l.77.771A5.944 5.944 0 0 1 8 3.5c2.12 0 3.879 1.168 5.168 2.457A13.134 13.134 0 0 1 14.828 8c-.058.087-.122.183-.195.288-.335.48-.83 1.12-1.465 1.755-.165.165-.337.328-.517.486l.708.709z"/><path d="M11.297 9.176a3.5 3.5 0 0 0-4.474-4.474l.823.823a2.5 2.5 0 0 1 2.829 2.829l.822.822zm-2.943 1.299.822.822a3.5 3.5 0 0 1-4.474-4.474l.823.823a2.5 2.5 0 0 0 2.829 2.829z"/><path d="M3.35 5.47c-.18.16-.353.322-.518.487A13.134 13.134 0 0 0 1.172 8l.195.288c.335.48.83 1.12 1.465 1.755C4.121 11.332 5.881 12.5 8 12.5c.716 0 1.39-.133 2.02-.36l.77.772A7.029 7.029 0 0 1 8 13.5C3 13.5 0 8 0 8s.939-1.721 2.641-3.238l.708.709zm10.296 8.884-12-12 .708-.708 12 12-.708.708z"/></svg>';
+    $('.wifi-eye').html(WIFI_EYEOFF);
+    // Mask WiFi keys with CSS on a type=text input where supported: the browser's password
+    // manager then never sees a password field, so it doesn't offer to save SSID+key as a
+    // login (autocomplete=off/new-password does not reliably stop that). The inputs are
+    // ALREADY type=text in the HTML — a field that has ever been type=password stays on the
+    // password manager's radar even after switching to text. Fallback: type=password.
+    var wifiPwdCssMask = window.CSS && CSS.supports && CSS.supports('-webkit-text-security', 'disc');
+    function wifiPwdSetMasked(inp, masked) {
+        if (wifiPwdCssMask) inp.attr('type', 'text').toggleClass('wifi-pwd-mask', masked);
+        else inp.attr('type', masked ? 'password' : 'text');
+    }
+    function wifiPwdIsMasked(inp) {
+        return wifiPwdCssMask ? inp.hasClass('wifi-pwd-mask') : inp.attr('type') === 'password';
+    }
+    wifiPwdSetMasked($('#wifi-cm-pwd'), true);
+    wifiPwdSetMasked($('#wifi-ap-pwd'), true);
+    // A backend restart while the page stays up still makes a fallback browser (no CSS
+    // masking, real type=password) take a password-save snapshot when the socket drops.
+    // Stash the connect-dialog key into JS — invisible to the password manager — blank the
+    // DOM field, and put it back on reconnect: nothing typed is lost. The AP password field
+    // is deliberately left untouched: it holds the STORED AP key (pre-filled from the
+    // server) that the user must still be able to reveal while the backend is down, and
+    // variant A (type=text + CSS mask) already keeps it off the password manager's radar in
+    // Chrome/Edge/Safari.
+    socket.on('disconnect', function () {
+        wifi.pwdStash = {cm: $('#wifi-cm-pwd').val()};
+        $('#wifi-cm-pwd').val('');
+    });
+    socket.on('connect', function () {
+        if (!wifi.pwdStash) return;
+        $('#wifi-cm-pwd').val(wifi.pwdStash.cm);
+        wifi.pwdStash = null;
+    });
+    $('#wifi-cm-eye').on('click', function () {
+        var inp = $('#wifi-cm-pwd'), show = wifiPwdIsMasked(inp);
+        wifiPwdSetMasked(inp, !show);
+        $(this).html(show ? WIFI_EYE : WIFI_EYEOFF);
+    });
+    $('#wifi-cm-pwd').on('input', function () { wifiShowPwdErr(''); });   // clear the inline error as the user types
+    // hidden-network dialog: typing an SSID that is in fact visible in the scan creates a
+    // duplicate (unpinned) profile next to the pinned one — warn, don't block
+    $('#wifi-cm-ssid').on('input', function () {
+        var ssid = $(this).val().trim(), seen = false;
+        if (ssid && wifi.list) {
+            var all = (wifi.list.connected || []).concat(wifi.list.saved || [], wifi.list.available || []);
+            for (var i = 0; i < all.length; i++) if (all[i].ssid === ssid && all[i].bssid) { seen = true; break; }
+        }
+        $('#wifi-cm-ssid-visible').toggle(seen);
+        // A hidden network answers a probe addressed to its name, and the answer says which
+        // security it uses — so the operator does not have to know. Asked once the
+        // typing stops; the selector stays editable, the answer only fills it in.
+        clearTimeout(wifi.probeTimer);
+        $('#wifi-cm-sec-detected').hide();
+        // always reset: a probe already in flight for the previous text must not come back
+        // and silently rewrite the Security selector for a field that no longer holds it
+        wifi.probeSsid = '';
+        if (!ssid || ssid.length < 2) return;
+        wifi.probeSsid = ssid;
+        wifi.probeTimer = setTimeout(function () {
+            socket.emit('wifi probe hidden', {ssid: ssid});
+        }, 700);
+    });
+    $('#wifi-cm-sec').on('change', function () {
+        $('#wifi-cm-pwd-field').toggle($(this).val() !== 'open');
+        wifiPwdSetHint($(this).val());
+    });
+    $('#wifi-cm-ipmode').on('change', 'input', function () {
+        $('#wifi-cm-static').toggle($(this).val() === 'manual');
+        $('#wifi-cm-autodns').toggle($(this).val() !== 'manual');
+    });
+    $('#wifi-cm-ip, #wifi-cm-gw, #wifi-cm-dns').on('input', function () {
+        wifiValidateStatic({ip: 'wifi-cm-ip', mask: 'wifi-cm-mask', gw: 'wifi-cm-gw', dns: 'wifi-cm-dns', err: 'wifi-cm-ip-err', softOccupied: true});
+    });
+    $('#wifi-cm-mask').on('change', function () {
+        wifiValidateStatic({ip: 'wifi-cm-ip', mask: 'wifi-cm-mask', gw: 'wifi-cm-gw', dns: 'wifi-cm-dns', err: 'wifi-cm-ip-err', softOccupied: true});
+    });
+
+    // AP form events
+    $('#wifi-ap-band').on('change', function () {
+        if (!wifi.updating) wifiRepopRadio();
+    });
+    $('#wifi-ap-channel').on('change', function () {
+        // the warning describes the STORED channel — moving away from it makes it stale
+        if (!wifi.updating && +$(this).val() !== wifi.apStoredChannel) $('#wifi-ap-chan-warn').hide();
+    });
+    $('#wifi-ap-sec').on('change', function () {
+        $('#wifi-ap-pwd-field').toggle($(this).val() !== 'open');
+    });
+    $('#wifi-ap-eye').on('click', function () {
+        var inp = $('#wifi-ap-pwd'), show = wifiPwdIsMasked(inp);
+        wifiPwdSetMasked(inp, !show);
+        $(this).html(show ? WIFI_EYE : WIFI_EYEOFF);
+    });
+    $('#wifi-ap-ip').on('input', function () {
+        wifiValidateStatic({ip: 'wifi-ap-ip', mask: 'wifi-ap-mask', err: 'wifi-ap-ip-err', selfDevice: 'ap0'});
+    });
+    $('#wifi-ap-mask').on('change', function () {
+        wifiValidateStatic({ip: 'wifi-ap-ip', mask: 'wifi-ap-mask', err: 'wifi-ap-ip-err', selfDevice: 'ap0'});
+    });
+
+    function wifiApAutoNote() {
+        // one quiet informational line under the card header; never an error tone —
+        // "auto on, nothing saved yet" is a normal state on a freshly provisioned station
+        var note = '';
+        if (wifi.apSingleIface) {
+            note = 'The access point is running on wlan0 (single-interface mode) — '
+                 + 'client connections and Auto-start are unavailable.';
+        } else if (wifi.apPendingAuto) {
+            note = 'Auto-start will be armed when you press Save below.';
+        } else if (wifi.apStoredAuto && !wifi.apConfigured) {
+            note = 'Auto-start is on, but not active yet — set up and Save the access point '
+                 + 'below, and Auto-start will have something to raise.';
+        } else if ($('#wifi-ap-switch').prop('checked') && !wifi.apStoredEnabled) {
+            note = 'The access point will start when you press Save & Start below.';
+        }
+        $('#wifi-ap-auto-note').text(note).toggle(!!note);
+    }
+
+    function wifiApSyncSwitches() {
+        // programmatic sync (no submit): the switches mirror the stored config + pending.
+        // The point's switch also reads ON while the operator holds the sleeping point's
+        // form open — flipping it on opens the form for editing, it no longer promises a
+        // start (that intent lives in the Save & Start button)
+        wifi.updating = true;
+        $('#wifi-ap-switch').prop('checked', !!wifi.apStoredEnabled || !!wifi.apFormOpen);
+        $('#wifi-ap-auto-switch').prop('checked', !!wifi.apStoredAuto || !!wifi.apPendingAuto);
+        wifi.updating = false;
+        // the form shows only while the point's own switch is on — an off point hides
+        // its form, as it always did
+        $('#wifi-ap-body').toggle($('#wifi-ap-switch').prop('checked'));
+        // which save buttons make sense depends on the air: a sleeping point is written
+        // silently (Save) or raised (Save & Start); a live one restarts on save
+        $('#wifi-ap-save').toggle(!wifi.apOnAir);
+        $('#wifi-ap-start').text(wifi.apOnAir ? 'Save & Restart' : 'Save & Start');
+        wifiApAutoNote();
+    }
+
+    function wifiSubmitAp(enabled, auto, saveOnly) {
+        // Form validation gates starting the point, arming Auto and the plain profile
+        // write (the backend test-starts the point when the armed watchdog's start-fields
+        // change, so those settings need everything a real start needs, region included).
+        // Submits that only switch things OFF must always go through — even from an
+        // out-of-band state (AP brought up by hand, no region, odd key) — or the UI cannot
+        // leave that state. The backend then keeps the stored profile untouched when the
+        // submitted values are invalid.
+        var reject = function (msg) {
+            wifiToast(msg, true);
+            wifiApSyncSwitches();
+        };
+        if (enabled || auto || saveOnly) {
+            // a live point is legitimately editable with no region (limited world-safe
+            // mode) — the gate is only for bringing up a silent one
+            if (!(wifi.status && wifi.status.country) && !wifi.apOnAir) {
+                return reject('Select a WiFi region (country) for the access point');
+            }
+            if (!wifiValidateStatic({ip: 'wifi-ap-ip', mask: 'wifi-ap-mask', err: 'wifi-ap-ip-err', selfDevice: 'ap0'})) {
+                return reject('Check the access point IP / mask');
+            }
+            var pwd = $('#wifi-ap-pwd').val();
+            if ($('#wifi-ap-sec').val() !== 'open') {
+                if (pwd ? (pwd.length < 8 || pwd.length > 63) : !wifi.apHasPassword) {
+                    return reject('AP password must be 8–63 characters');
+                }
+            }
+        }
+        var cfg = wifiGatherApForm(!!enabled);
+        cfg.auto = !!auto;       // Save records the Auto switch, whatever its position
+        if (saveOnly) cfg.save_only = true;   // write the profile, the point sleeps on
+        wifiSetBusy(true,
+            enabled ? 'Saving and starting access point…'
+                : saveOnly ? 'Saving access point…'
+                : auto ? 'Saving and test-starting access point…'
+                : 'Saving access point…',
+            enabled ? 'Access point started'
+                : saveOnly ? 'Access point saved'
+                : auto ? 'Access point set to start automatically'
+                : 'Access point saved',
+            'ap');
+        socket.emit('wifi set ap', cfg);
+    }
+
+    $('#wifi-ap-switch').on('change', function () {
+        if (wifi.updating) return;
+        $('#wifi-ap-body').toggle(this.checked);
+        if (this.checked) {
+            // turning ON opens the sleeping point's form for editing — nothing is
+            // pending yet: Save writes the profile, Save & Start raises the point
+            wifi.apFormOpen = true;
+            wifiApAutoNote();
+            return;
+        }
+        wifi.apFormOpen = false;
+        wifiApAutoNote();
+        // turning OFF applies immediately and never carries `auto` — the switches are
+        // independent, so flipping the point off must not disarm the watchdog
+        var apRunning = !!(wifi.status && (wifi.status.ap_active || wifi.status.role === 'ap'));
+        if (!apRunning && !wifi.apStoredEnabled) return;   // flipped on and back, nothing to undo
+        var cfg = wifiGatherApForm(false);
+        wifiSetBusy(true, 'Turning access point off…', 'Access point turned off', 'ap');
+        socket.emit('wifi set ap', cfg);
+    });
+
+    $('#wifi-ap-auto-switch').on('change', function () {
+        if (wifi.updating) return;
+        if (!this.checked) {
+            if (wifi.apPendingAuto) {          // not armed yet — the click is simply undone
+                wifi.apPendingAuto = false;
+                wifiApAutoNote();
+                return;
+            }
+            // disarming applies immediately; the watchdog's own point goes down with it
+            // (a disarmed watchdog would never lower it) — warn when that beacon may be
+            // the very link this page came through
+            if (wifi.apOnAir && wifi.apRaisedByAuto &&
+                !window.confirm('Auto-start raised the access point that is online now. '
+                    + 'Turning Auto-start off stops that access point — if you are connected '
+                    + 'through it, you will lose this page. Continue?')) {
+                wifiApSyncSwitches();
+                return;
+            }
+            wifiSetBusy(true, 'Turning Auto-start off…', 'Auto-start turned off', 'ap');
+            socket.emit('wifi set ap auto', {auto: false});
+            return;
+        }
+        // turning ON — only next to a point that is online right now, or one being turned
+        // on in this very form. The watchdog is never armed with a point that has not
+        // proven it can stand.
+        if (wifi.apOnAir) {
+            // 1. live beacon: the settings are proven on the air — arm immediately
+            wifiSetBusy(true, 'Turning Auto-start on…', 'Auto-start turned on', 'ap');
+            socket.emit('wifi set ap auto', {auto: true});
+            return;
+        }
+        if ($('#wifi-ap-switch').prop('checked')) {
+            // 2. the point's form is open: the flag rides with Save / Save & Start and is
+            // recorded only after the point proves it can stand (a real or test start) —
+            // a failed start leaves Auto-start off
+            wifi.apPendingAuto = true;
+            wifi.apDirty = true;
+            wifiApAutoNote();
+            return;
+        }
+        // the point's form is closed: there is nothing for Auto-start to arm against
+        this.checked = false;
+        wifiApAutoNote();
+        wifiToast('Auto-start arms only with the access point open — turn the switch on and press Save, or arm while the point is online', true);
+    });
+
+    function wifiApChannelClash(chosen) {
+        // One radio, one channel: an explicitly chosen channel wins over a client
+        // connection on another one, and the connection goes. That used to happen
+        // silently. Returns true when the operator declined.
+        var conn = (wifi.list && (wifi.list.connected || [])[0]) || null;
+        return chosen && conn && conn.chan && conn.chan !== chosen &&
+            !confirm('The station is connected to "' + conn.ssid + '" on channel ' + conn.chan +
+                     '. The access point on channel ' + chosen + ' will drop that connection — ' +
+                     'the adapter has a single channel.\n\nChoose channel Auto to let the access point ' +
+                     'follow the network instead. Continue?');
+    }
+
+    $('#wifi-ap-start').on('click', function () {
+        // Save & Start / Save & Restart: the point comes up (or restarts) with the
+        // settings below — the pre-114 Save, under an honest name
+        var auto = $('#wifi-ap-auto-switch').prop('checked');
+        if (wifiApChannelClash(+($('#wifi-ap-channel').val() || 0))) return;
+        wifi.apDirty = false;   // the edits are being saved — no discard warning needed
+        wifi.apPendingAuto = false;
+        wifiSubmitAp(true, auto);
+    });
+
+    $('#wifi-ap-save').on('click', function () {
+        // Save: write the profile, the sleeping point sleeps on. With Auto armed the
+        // backend still proves changed start-fields (band/channel/security) with a brief
+        // test start — only then can a chosen channel drop a client connection, so the
+        // clash warning fires only for that case.
+        var auto = $('#wifi-ap-auto-switch').prop('checked');
+        var chosen = +($('#wifi-ap-channel').val() || 0);
+        var startFieldsChanged = chosen !== wifi.apStoredChannel ||
+            ($('#wifi-ap-band').val() || 'all') !== wifi.apStoredBand ||
+            $('#wifi-ap-sec').val() !== wifi.apStoredSecurity;
+        if (auto && startFieldsChanged && wifiApChannelClash(chosen)) return;
+        wifi.apDirty = false;
+        wifi.apPendingAuto = false;
+        wifiSubmitAp(false, auto, true);
+    });
+    // Unsaved-changes guard: the AP form applies on Save only, so closing the dialog with
+    // edits pending silently discards them. Track user edits (programmatic re-renders run
+    // under wifi.updating) and confirm before the modal closes.
+    $('#wifi-ap-body').on('input change', 'input, select', function () {
+        if (!wifi.updating) wifi.apDirty = true;
+    });
+    $('#wifiModal').on('hide.bs.modal', function (e) {
+        // pending Auto counts as an unsaved edit: it only arms on Save
+        if ((wifi.apDirty || wifi.apPendingAuto) &&
+            !window.confirm('Access point settings were changed but not saved. Close and discard the changes?')) {
+            e.preventDefault();
+            return;
+        }
+        wifi.apDirty = false;
+        wifi.apPendingAuto = false;
+        wifi.apFormOpen = false;   // the sleeping point's form closes with the dialog
+        // re-sync the DOM right here: the closed dialog must not keep the form visually
+        // open, or the next opening shows the stale open form until the first render
+        // arrives (which waits behind the opening rescan)
+        wifiApSyncSwitches();
+    });
+
+    // optional WiFi profiles backup/restore (JSON)
+    $('#wifi-backup-button').on('click', function () { socket.emit('wifi backup'); });
+    $('#wifi-import-button').on('click', function () { $('#wifi-restore-file').click(); });
+    $('#wifi-restore-file').on('change', function () {
+        var file = this.files[0];
+        this.value = '';
+        if (!file) return;
+        var reader = new FileReader();
+        reader.onload = function () {
+            var data;
+            try {
+                data = JSON.parse(reader.result);
+            } catch (err) {
+                wifiToast('Invalid JSON file', true);
+                return;
+            }
+            // lock the dialog until the import finishes (it can rewrite profiles and apply
+            // the region); cleared by the 'wifi restore result' handler
+            wifiSetBusy(true, 'Importing WiFi settings…', '', 'restore');
+            socket.emit('wifi restore', data);
+        };
+        reader.readAsText(file);
+    });
+
     // end of document.ready
 });
