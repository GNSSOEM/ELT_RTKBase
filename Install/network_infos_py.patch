diff --git a/web_app/network_infos.py b/web_app/network_infos.py
index f71b92e..98e4f46 100644
--- a/web_app/network_infos.py
+++ b/web_app/network_infos.py
@@ -7,6 +7,7 @@ import logging
 import psutil
 import nmcli
 import argparse
+import socket
 
 logging.basicConfig(format='%(levelname)s: %(message)s')
 log = logging.getLogger(__name__)
@@ -31,6 +32,8 @@ def get_conn_name(device):
     except nmcli.NotExistException:
         log.debug("No connection name for {}".format(device))
         return None
+    except FileNotFoundError:
+        return None
 
 def get_up_if():
     """
@@ -44,7 +47,7 @@ def get_up_if():
     if_stats.pop('lo')
     log.debug(if_stats)
     if_stats = {k:v for (k,v) in if_stats.items() if if_stats[k].isup} # keep only up interface
-    if_stats = {k:v for (k,v) in if_stats.items() if if_stats[k].speed > 0 or 'pointopoint' in if_stats[k].flags} # keep only interface with speed > 0
+    if_stats = {k:v for (k,v) in if_stats.items() if if_stats[k].speed > 0 or 'pointopoint' in if_stats[k].flags or k.startswith('wlan')} # keep only interface with speed > 0
     if_stats = {k:v for (k,v) in if_stats.items() if not k.startswith('docker')} # remove docker interface
     return if_stats
 
@@ -86,10 +89,45 @@ def get_interfaces_infos():
         conn_name = get_conn_name(k)
         if conn_name:
             device_info["conn_name"] = conn_name
-        device_info["hwaddr"] = nmcli.device.show(k).get('GENERAL.HWADDR')
-        interfaces_infos.append(device_info)
+        try:
+            hwaddr = nmcli.device.show(k).get('GENERAL.HWADDR')
+            if hwaddr != "(unknown)":
+               device_info["hwaddr"] = hwaddr
+        except Exception:
+            hwaddr = None
+
+        if (len(ipv4) > 0) or (len(ipv6) > 0):
+            interfaces_infos.append(device_info)
     return interfaces_infos
 
+def get_primary_ip():
+    """
+    Return the first non-loopback IPv4 address.
+    Prefer an active interface.
+    """
+    for interface_name, addresses in psutil.net_if_addrs().items():
+        # Skip loopback and virtual interfaces when appropriate
+        if interface_name == "lo":
+            continue
+
+        stats = psutil.net_if_stats().get(interface_name)
+        if stats is not None and not stats.isup:
+            continue
+
+        for address in addresses:
+            if (address.family != socket.AF_INET) and (address.family != socket.AF_INET6):
+                continue
+
+            ip = address.address
+            if ip.startswith("127."):
+                continue
+            if ip.startswith("fe80"):
+                continue
+
+            return ip
+
+    return "no_ip"
+
 def arg_parse():
     """ Parse the command line you use to launch the script """
     
